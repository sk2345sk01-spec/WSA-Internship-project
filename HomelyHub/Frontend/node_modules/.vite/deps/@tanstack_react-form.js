import {
  require_jsx_runtime
} from "./chunk-OLBJ6L5L.js";
import {
  require_react
} from "./chunk-HJEDEILD.js";
import {
  __commonJS,
  __privateAdd,
  __privateGet,
  __privateSet,
  __privateWrapper,
  __toESM
} from "./chunk-VSE7GGL6.js";

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js
var require_use_sync_external_store_shim_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim.development.js"(exports) {
    "use strict";
    (function() {
      function is(x, y) {
        return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
      }
      function useSyncExternalStore$2(subscribe, getSnapshot) {
        didWarnOld18Alpha || void 0 === React2.startTransition || (didWarnOld18Alpha = true, console.error(
          "You are using an outdated, pre-release alpha of React 18 that does not support useSyncExternalStore. The use-sync-external-store shim will not work correctly. Upgrade to a newer pre-release."
        ));
        var value = getSnapshot();
        if (!didWarnUncachedGetSnapshot) {
          var cachedValue = getSnapshot();
          objectIs(value, cachedValue) || (console.error(
            "The result of getSnapshot should be cached to avoid an infinite loop"
          ), didWarnUncachedGetSnapshot = true);
        }
        cachedValue = useState8({
          inst: { value, getSnapshot }
        });
        var inst = cachedValue[0].inst, forceUpdate = cachedValue[1];
        useLayoutEffect2(
          function() {
            inst.value = value;
            inst.getSnapshot = getSnapshot;
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
          },
          [subscribe, value, getSnapshot]
        );
        useEffect2(
          function() {
            checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            return subscribe(function() {
              checkIfSnapshotChanged(inst) && forceUpdate({ inst });
            });
          },
          [subscribe]
        );
        useDebugValue(value);
        return value;
      }
      function checkIfSnapshotChanged(inst) {
        var latestGetSnapshot = inst.getSnapshot;
        inst = inst.value;
        try {
          var nextValue = latestGetSnapshot();
          return !objectIs(inst, nextValue);
        } catch (error) {
          return true;
        }
      }
      function useSyncExternalStore$1(subscribe, getSnapshot) {
        return getSnapshot();
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React2 = require_react(), objectIs = "function" === typeof Object.is ? Object.is : is, useState8 = React2.useState, useEffect2 = React2.useEffect, useLayoutEffect2 = React2.useLayoutEffect, useDebugValue = React2.useDebugValue, didWarnOld18Alpha = false, didWarnUncachedGetSnapshot = false, shim = "undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement ? useSyncExternalStore$1 : useSyncExternalStore$2;
      exports.useSyncExternalStore = void 0 !== React2.useSyncExternalStore ? React2.useSyncExternalStore : shim;
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/index.js
var require_shim = __commonJS({
  "node_modules/use-sync-external-store/shim/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_use_sync_external_store_shim_development();
    }
  }
});

// node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js
var require_with_selector_development = __commonJS({
  "node_modules/use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js"(exports) {
    "use strict";
    (function() {
      function is(x, y) {
        return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
      }
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStart(Error());
      var React2 = require_react(), shim = require_shim(), objectIs = "function" === typeof Object.is ? Object.is : is, useSyncExternalStore = shim.useSyncExternalStore, useRef2 = React2.useRef, useEffect2 = React2.useEffect, useMemo6 = React2.useMemo, useDebugValue = React2.useDebugValue;
      exports.useSyncExternalStoreWithSelector = function(subscribe, getSnapshot, getServerSnapshot, selector, isEqual) {
        var instRef = useRef2(null);
        if (null === instRef.current) {
          var inst = { hasValue: false, value: null };
          instRef.current = inst;
        } else inst = instRef.current;
        instRef = useMemo6(
          function() {
            function memoizedSelector(nextSnapshot) {
              if (!hasMemo) {
                hasMemo = true;
                memoizedSnapshot = nextSnapshot;
                nextSnapshot = selector(nextSnapshot);
                if (void 0 !== isEqual && inst.hasValue) {
                  var currentSelection = inst.value;
                  if (isEqual(currentSelection, nextSnapshot))
                    return memoizedSelection = currentSelection;
                }
                return memoizedSelection = nextSnapshot;
              }
              currentSelection = memoizedSelection;
              if (objectIs(memoizedSnapshot, nextSnapshot))
                return currentSelection;
              var nextSelection = selector(nextSnapshot);
              if (void 0 !== isEqual && isEqual(currentSelection, nextSelection))
                return memoizedSnapshot = nextSnapshot, currentSelection;
              memoizedSnapshot = nextSnapshot;
              return memoizedSelection = nextSelection;
            }
            var hasMemo = false, memoizedSnapshot, memoizedSelection, maybeGetServerSnapshot = void 0 === getServerSnapshot ? null : getServerSnapshot;
            return [
              function() {
                return memoizedSelector(getSnapshot());
              },
              null === maybeGetServerSnapshot ? void 0 : function() {
                return memoizedSelector(maybeGetServerSnapshot());
              }
            ];
          },
          [getSnapshot, getServerSnapshot, selector, isEqual]
        );
        var value = useSyncExternalStore(subscribe, instRef[0], instRef[1]);
        useEffect2(
          function() {
            inst.hasValue = true;
            inst.value = value;
          },
          [value]
        );
        useDebugValue(value);
        return value;
      };
      "undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ && "function" === typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop && __REACT_DEVTOOLS_GLOBAL_HOOK__.registerInternalModuleStop(Error());
    })();
  }
});

// node_modules/use-sync-external-store/shim/with-selector.js
var require_with_selector = __commonJS({
  "node_modules/use-sync-external-store/shim/with-selector.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_with_selector_development();
    }
  }
});

// node_modules/@tanstack/store/dist/alien.js
function createReactiveSystem({ update, notify, unwatched }) {
  return {
    link: link2,
    unlink: unlink2,
    propagate: propagate2,
    checkDirty: checkDirty2,
    shallowPropagate: shallowPropagate2
  };
  function link2(dep, sub, version2) {
    const prevDep = sub.depsTail;
    if (prevDep !== void 0 && prevDep.dep === dep) return;
    const nextDep = prevDep !== void 0 ? prevDep.nextDep : sub.deps;
    if (nextDep !== void 0 && nextDep.dep === dep) {
      nextDep.version = version2;
      sub.depsTail = nextDep;
      return;
    }
    const prevSub = dep.subsTail;
    if (prevSub !== void 0 && prevSub.version === version2 && prevSub.sub === sub) return;
    const newLink = sub.depsTail = dep.subsTail = {
      version: version2,
      dep,
      sub,
      prevDep,
      nextDep,
      prevSub,
      nextSub: void 0
    };
    if (nextDep !== void 0) nextDep.prevDep = newLink;
    if (prevDep !== void 0) prevDep.nextDep = newLink;
    else sub.deps = newLink;
    if (prevSub !== void 0) prevSub.nextSub = newLink;
    else dep.subs = newLink;
  }
  function unlink2(link3, sub = link3.sub) {
    const dep = link3.dep;
    const prevDep = link3.prevDep;
    const nextDep = link3.nextDep;
    const nextSub = link3.nextSub;
    const prevSub = link3.prevSub;
    if (nextDep !== void 0) nextDep.prevDep = prevDep;
    else sub.depsTail = prevDep;
    if (prevDep !== void 0) prevDep.nextDep = nextDep;
    else sub.deps = nextDep;
    if (nextSub !== void 0) nextSub.prevSub = prevSub;
    else dep.subsTail = prevSub;
    if (prevSub !== void 0) prevSub.nextSub = nextSub;
    else if ((dep.subs = nextSub) === void 0) unwatched(dep);
    return nextDep;
  }
  function propagate2(link3) {
    let next = link3.nextSub;
    let stack;
    top: do {
      const sub = link3.sub;
      let flags = sub.flags;
      if (!(flags & 60)) sub.flags = flags | 32;
      else if (!(flags & (4 | 8))) flags = 0;
      else if (!(flags & 4)) sub.flags = flags & ~8 | 32;
      else if (!(flags & (16 | 32)) && isValidLink(link3, sub)) {
        sub.flags = flags | (8 | 32);
        flags &= 1;
      } else flags = 0;
      if (flags & 2) notify(sub);
      if (flags & 1) {
        const subSubs = sub.subs;
        if (subSubs !== void 0) {
          const nextSub = (link3 = subSubs).nextSub;
          if (nextSub !== void 0) {
            stack = {
              value: next,
              prev: stack
            };
            next = nextSub;
          }
          continue;
        }
      }
      if ((link3 = next) !== void 0) {
        next = link3.nextSub;
        continue;
      }
      while (stack !== void 0) {
        link3 = stack.value;
        stack = stack.prev;
        if (link3 !== void 0) {
          next = link3.nextSub;
          continue top;
        }
      }
      break;
    } while (true);
  }
  function checkDirty2(link3, sub) {
    let stack;
    let checkDepth = 0;
    let dirty = false;
    top: do {
      const dep = link3.dep;
      const flags = dep.flags;
      if (sub.flags & 16) dirty = true;
      else if ((flags & (1 | 16)) === (1 | 16)) {
        if (update(dep)) {
          const subs = dep.subs;
          if (subs.nextSub !== void 0) shallowPropagate2(subs);
          dirty = true;
        }
      } else if ((flags & (1 | 32)) === (1 | 32)) {
        if (link3.nextSub !== void 0 || link3.prevSub !== void 0) stack = {
          value: link3,
          prev: stack
        };
        link3 = dep.deps;
        sub = dep;
        ++checkDepth;
        continue;
      }
      if (!dirty) {
        const nextDep = link3.nextDep;
        if (nextDep !== void 0) {
          link3 = nextDep;
          continue;
        }
      }
      while (checkDepth--) {
        const firstSub = sub.subs;
        const hasMultipleSubs = firstSub.nextSub !== void 0;
        if (hasMultipleSubs) {
          link3 = stack.value;
          stack = stack.prev;
        } else link3 = firstSub;
        if (dirty) {
          if (update(sub)) {
            if (hasMultipleSubs) shallowPropagate2(firstSub);
            sub = link3.sub;
            continue;
          }
          dirty = false;
        } else sub.flags &= ~32;
        sub = link3.sub;
        const nextDep = link3.nextDep;
        if (nextDep !== void 0) {
          link3 = nextDep;
          continue top;
        }
      }
      return dirty;
    } while (true);
  }
  function shallowPropagate2(link3) {
    do {
      const sub = link3.sub;
      const flags = sub.flags;
      if ((flags & (32 | 16)) === 32) {
        sub.flags = flags | 16;
        if ((flags & (2 | 4)) === 2) notify(sub);
      }
    } while ((link3 = link3.nextSub) !== void 0);
  }
  function isValidLink(checkLink, sub) {
    let link3 = sub.depsTail;
    while (link3 !== void 0) {
      if (link3 === checkLink) return true;
      link3 = link3.prevDep;
    }
    return false;
  }
}

// node_modules/@tanstack/store/dist/atom.js
function toObserver(nextHandler, errorHandler, completionHandler) {
  var _a2, _b, _c;
  const isObserver = typeof nextHandler === "object";
  const self = isObserver ? nextHandler : void 0;
  return {
    next: (_a2 = isObserver ? nextHandler.next : nextHandler) == null ? void 0 : _a2.bind(self),
    error: (_b = isObserver ? nextHandler.error : errorHandler) == null ? void 0 : _b.bind(self),
    complete: (_c = isObserver ? nextHandler.complete : completionHandler) == null ? void 0 : _c.bind(self)
  };
}
var queuedEffects = [];
var cycle = 0;
var { link, unlink, propagate, checkDirty, shallowPropagate } = createReactiveSystem({
  update(atom) {
    return atom._update();
  },
  notify(effect2) {
    queuedEffects[queuedEffectsLength++] = effect2;
    effect2.flags &= ~2;
  },
  unwatched(atom) {
    if (atom.depsTail !== void 0) {
      atom.depsTail = void 0;
      atom.flags = 1 | 16;
      purgeDeps(atom);
    }
  }
});
var notifyIndex = 0;
var queuedEffectsLength = 0;
var activeSub;
var batchDepth = 0;
function batch(fn) {
  try {
    ++batchDepth;
    fn();
  } finally {
    if (!--batchDepth) flush();
  }
}
function purgeDeps(sub) {
  const depsTail = sub.depsTail;
  let dep = depsTail !== void 0 ? depsTail.nextDep : sub.deps;
  while (dep !== void 0) dep = unlink(dep, sub);
}
function flush() {
  if (batchDepth > 0) return;
  while (notifyIndex < queuedEffectsLength) {
    const effect2 = queuedEffects[notifyIndex];
    queuedEffects[notifyIndex++] = void 0;
    effect2.notify();
  }
  notifyIndex = 0;
  queuedEffectsLength = 0;
}
function createAtom(valueOrFn, options) {
  const isComputed = typeof valueOrFn === "function";
  const getter = valueOrFn;
  const atom = {
    _snapshot: isComputed ? void 0 : valueOrFn,
    subs: void 0,
    subsTail: void 0,
    deps: void 0,
    depsTail: void 0,
    flags: isComputed ? 0 : 1,
    get() {
      if (activeSub !== void 0) link(atom, activeSub, cycle);
      return atom._snapshot;
    },
    subscribe(observerOrFn) {
      const obs = toObserver(observerOrFn);
      const observed = { current: false };
      const e = effect(() => {
        var _a2;
        atom.get();
        if (!observed.current) observed.current = true;
        else (_a2 = obs.next) == null ? void 0 : _a2.call(obs, atom._snapshot);
      });
      return { unsubscribe: () => {
        e.stop();
      } };
    },
    _update(getValue) {
      const prevSub = activeSub;
      const compare = (options == null ? void 0 : options.compare) ?? Object.is;
      if (isComputed) {
        activeSub = atom;
        ++cycle;
        atom.depsTail = void 0;
      } else if (getValue === void 0) return false;
      if (isComputed) atom.flags = 1 | 4;
      try {
        const oldValue = atom._snapshot;
        const newValue = typeof getValue === "function" ? getValue(oldValue) : getValue === void 0 && isComputed ? getter(oldValue) : getValue;
        if (oldValue === void 0 || !compare(oldValue, newValue)) {
          atom._snapshot = newValue;
          return true;
        }
        return false;
      } finally {
        activeSub = prevSub;
        if (isComputed) atom.flags &= ~4;
        purgeDeps(atom);
      }
    }
  };
  if (isComputed) {
    atom.flags = 1 | 16;
    atom.get = function() {
      const flags = atom.flags;
      if (flags & 16 || flags & 32 && checkDirty(atom.deps, atom)) {
        if (atom._update()) {
          const subs = atom.subs;
          if (subs !== void 0) shallowPropagate(subs);
        }
      } else if (flags & 32) atom.flags = flags & ~32;
      if (activeSub !== void 0) link(atom, activeSub, cycle);
      return atom._snapshot;
    };
  } else atom.set = function(valueOrFn2) {
    if (atom._update(valueOrFn2)) {
      const subs = atom.subs;
      if (subs !== void 0) {
        propagate(subs);
        shallowPropagate(subs);
        flush();
      }
    }
  };
  return atom;
}
function effect(fn) {
  const run = () => {
    const prevSub = activeSub;
    activeSub = effectObj;
    ++cycle;
    effectObj.depsTail = void 0;
    effectObj.flags = 2 | 4;
    try {
      return fn();
    } finally {
      activeSub = prevSub;
      effectObj.flags &= ~4;
      purgeDeps(effectObj);
    }
  };
  const effectObj = {
    deps: void 0,
    depsTail: void 0,
    subs: void 0,
    subsTail: void 0,
    flags: 2 | 4,
    notify() {
      const flags = this.flags;
      if (flags & 16 || flags & 32 && checkDirty(this.deps, this)) run();
      else this.flags = 2;
    },
    stop() {
      this.flags = 0;
      this.depsTail = void 0;
      purgeDeps(this);
    }
  };
  run();
  return effectObj;
}

// node_modules/@tanstack/store/dist/store.js
var Store = class {
  constructor(valueOrFn, actionsFactory) {
    this.atom = createAtom(valueOrFn);
    this.get = this.get.bind(this);
    this.setState = this.setState.bind(this);
    this.subscribe = this.subscribe.bind(this);
    if (actionsFactory) this.actions = actionsFactory(this);
  }
  setState(updater) {
    this.atom.set(updater);
  }
  get state() {
    return this.atom.get();
  }
  get() {
    return this.state;
  }
  subscribe(observerOrFn) {
    return this.atom.subscribe(toObserver(observerOrFn));
  }
};
var ReadonlyStore = class {
  constructor(valueOrFn) {
    this.atom = createAtom(valueOrFn);
  }
  get state() {
    return this.atom.get();
  }
  get() {
    return this.state;
  }
  subscribe(observerOrFn) {
    return this.atom.subscribe(toObserver(observerOrFn));
  }
};
function createStore(valueOrFn, actions) {
  if (typeof valueOrFn === "function") return new ReadonlyStore(valueOrFn);
  if (actions) return new Store(valueOrFn, actions);
  return new Store(valueOrFn);
}

// node_modules/@tanstack/pacer-lite/dist/lite-throttler.js
var LiteThrottler = class {
  constructor(fn, options) {
    this.fn = fn;
    this.options = options;
    this.lastExecutionTime = 0;
    this.isPending = false;
    this.maybeExecute = (...args) => {
      const timeSinceLastExecution = Date.now() - this.lastExecutionTime;
      if (this.options.leading && timeSinceLastExecution >= this.options.wait) this.execute(...args);
      else {
        this.lastArgs = args;
        if (!this.timeoutId && this.options.trailing) {
          const timeoutDuration = this.options.wait - timeSinceLastExecution;
          this.isPending = true;
          this.timeoutId = setTimeout(() => {
            if (this.lastArgs !== void 0) this.execute(...this.lastArgs);
          }, timeoutDuration);
        }
      }
    };
    this.execute = (...args) => {
      var _a2, _b;
      this.fn(...args);
      (_b = (_a2 = this.options).onExecute) == null ? void 0 : _b.call(_a2, args, this);
      this.lastExecutionTime = Date.now();
      this.clearTimeout();
      this.lastArgs = void 0;
      this.isPending = false;
    };
    this.flush = () => {
      if (this.isPending && this.lastArgs) this.execute(...this.lastArgs);
    };
    this.cancel = () => {
      this.clearTimeout();
      this.lastArgs = void 0;
      this.isPending = false;
    };
    this.clearTimeout = () => {
      if (this.timeoutId) {
        clearTimeout(this.timeoutId);
        this.timeoutId = void 0;
      }
    };
    if (this.options.leading === void 0 && this.options.trailing === void 0) {
      this.options.leading = true;
      this.options.trailing = true;
    }
  }
};
function liteThrottle(fn, options) {
  return new LiteThrottler(fn, options).maybeExecute;
}

// node_modules/@tanstack/devtools-event-client/dist/esm/plugin.js
var _enabled, _pluginId, _eventTarget, _debug, _queuedEvents, _connected, _connectIntervalId, _connectEveryMs, _retryCount, _maxRetries, _connecting, _failedToConnect, _internalEventTarget, _onConnected, _retryConnection, _connectFunction, _a;
var EventClient = (_a = class {
  constructor({ pluginId, debug = false, enabled = true, reconnectEveryMs = 300 }) {
    __privateAdd(this, _enabled, true);
    __privateAdd(this, _pluginId);
    __privateAdd(this, _eventTarget);
    __privateAdd(this, _debug);
    __privateAdd(this, _queuedEvents);
    __privateAdd(this, _connected);
    __privateAdd(this, _connectIntervalId);
    __privateAdd(this, _connectEveryMs);
    __privateAdd(this, _retryCount, 0);
    __privateAdd(this, _maxRetries, 5);
    __privateAdd(this, _connecting, false);
    __privateAdd(this, _failedToConnect, false);
    __privateAdd(this, _internalEventTarget, null);
    __privateAdd(this, _onConnected, () => {
      this.debugLog("Connected to event bus");
      __privateSet(this, _connected, true);
      __privateSet(this, _connecting, false);
      this.debugLog("Emitting queued events", __privateGet(this, _queuedEvents));
      __privateGet(this, _queuedEvents).forEach((event) => this.emitEventToBus(event));
      __privateSet(this, _queuedEvents, []);
      this.stopConnectLoop();
      __privateGet(this, _eventTarget).call(this).removeEventListener("tanstack-connect-success", __privateGet(this, _onConnected));
    });
    __privateAdd(this, _retryConnection, () => {
      if (__privateGet(this, _retryCount) < __privateGet(this, _maxRetries)) {
        __privateWrapper(this, _retryCount)._++;
        this.dispatchCustomEvent("tanstack-connect", {});
        return;
      }
      __privateGet(this, _eventTarget).call(this).removeEventListener("tanstack-connect", __privateGet(this, _retryConnection));
      __privateSet(this, _failedToConnect, true);
      this.debugLog("Max retries reached, giving up on connection");
      this.stopConnectLoop();
    });
    __privateAdd(this, _connectFunction, () => {
      if (__privateGet(this, _connecting)) return;
      __privateSet(this, _connecting, true);
      __privateGet(this, _eventTarget).call(this).addEventListener("tanstack-connect-success", __privateGet(this, _onConnected));
      __privateGet(this, _retryConnection).call(this);
    });
    __privateSet(this, _pluginId, pluginId);
    __privateSet(this, _enabled, enabled);
    __privateSet(this, _eventTarget, this.getGlobalTarget);
    __privateSet(this, _debug, debug);
    this.debugLog(" Initializing event subscription for plugin", __privateGet(this, _pluginId));
    __privateSet(this, _queuedEvents, []);
    __privateSet(this, _connected, false);
    __privateSet(this, _failedToConnect, false);
    __privateSet(this, _connectIntervalId, null);
    __privateSet(this, _connectEveryMs, reconnectEveryMs);
  }
  startConnectLoop() {
    if (__privateGet(this, _connectIntervalId) !== null || __privateGet(this, _connected)) return;
    this.debugLog(`Starting connect loop (every ${__privateGet(this, _connectEveryMs)}ms)`);
    __privateSet(this, _connectIntervalId, setInterval(__privateGet(this, _retryConnection), __privateGet(this, _connectEveryMs)));
  }
  stopConnectLoop() {
    __privateSet(this, _connecting, false);
    if (__privateGet(this, _connectIntervalId) === null) return;
    clearInterval(__privateGet(this, _connectIntervalId));
    __privateSet(this, _connectIntervalId, null);
    __privateSet(this, _queuedEvents, []);
    this.debugLog("Stopped connect loop");
  }
  debugLog(...args) {
    if (__privateGet(this, _debug)) console.log(`🌴 [tanstack-devtools:${__privateGet(this, _pluginId)}-plugin]`, ...args);
  }
  getGlobalTarget() {
    if (typeof globalThis !== "undefined" && globalThis.__TANSTACK_EVENT_TARGET__) {
      this.debugLog("Using global event target");
      return globalThis.__TANSTACK_EVENT_TARGET__;
    }
    if (typeof window !== "undefined" && typeof window.addEventListener !== "undefined") {
      this.debugLog("Using window as event target");
      return window;
    }
    const eventTarget = typeof EventTarget !== "undefined" ? new EventTarget() : void 0;
    if (typeof eventTarget === "undefined" || typeof eventTarget.addEventListener === "undefined") {
      this.debugLog("No event mechanism available, running in non-web environment");
      return {
        addEventListener: () => {
        },
        removeEventListener: () => {
        },
        dispatchEvent: () => false
      };
    }
    this.debugLog("Using new EventTarget as fallback");
    return eventTarget;
  }
  getPluginId() {
    return __privateGet(this, _pluginId);
  }
  dispatchCustomEventShim(eventName, detail) {
    try {
      const event = new Event(eventName, { detail });
      __privateGet(this, _eventTarget).call(this).dispatchEvent(event);
    } catch (e) {
      this.debugLog("Failed to dispatch shim event");
    }
  }
  dispatchCustomEvent(eventName, detail) {
    try {
      __privateGet(this, _eventTarget).call(this).dispatchEvent(new CustomEvent(eventName, { detail }));
    } catch (e) {
      this.dispatchCustomEventShim(eventName, detail);
    }
  }
  emitEventToBus(event) {
    this.debugLog("Emitting event to client bus", event);
    this.dispatchCustomEvent("tanstack-dispatch-event", event);
  }
  createEventPayload(eventSuffix, payload) {
    return {
      type: `${__privateGet(this, _pluginId)}:${eventSuffix}`,
      payload,
      pluginId: __privateGet(this, _pluginId)
    };
  }
  emit(eventSuffix, payload) {
    if (!__privateGet(this, _enabled)) {
      this.debugLog("Event bus client is disabled, not emitting event", eventSuffix, payload);
      return;
    }
    if (__privateGet(this, _internalEventTarget)) {
      this.debugLog("Emitting event to internal event target", eventSuffix, payload);
      __privateGet(this, _internalEventTarget).dispatchEvent(new CustomEvent(`${__privateGet(this, _pluginId)}:${eventSuffix}`, { detail: this.createEventPayload(eventSuffix, payload) }));
    }
    if (__privateGet(this, _failedToConnect)) {
      this.debugLog("Previously failed to connect, not emitting to bus");
      return;
    }
    if (!__privateGet(this, _connected)) {
      this.debugLog("Bus not available, will be pushed as soon as connected");
      __privateGet(this, _queuedEvents).push(this.createEventPayload(eventSuffix, payload));
      if (typeof CustomEvent !== "undefined" && !__privateGet(this, _connecting)) {
        __privateGet(this, _connectFunction).call(this);
        this.startConnectLoop();
      }
      return;
    }
    return this.emitEventToBus(this.createEventPayload(eventSuffix, payload));
  }
  on(eventSuffix, cb, options) {
    const withEventTarget = (options == null ? void 0 : options.withEventTarget) ?? false;
    const eventName = `${__privateGet(this, _pluginId)}:${eventSuffix}`;
    if (withEventTarget) {
      if (!__privateGet(this, _internalEventTarget)) __privateSet(this, _internalEventTarget, new EventTarget());
      __privateGet(this, _internalEventTarget).addEventListener(eventName, (e) => {
        cb(e.detail);
      });
    }
    if (!__privateGet(this, _enabled)) {
      this.debugLog("Event bus client is disabled, not registering event", eventName);
      return () => {
      };
    }
    const handler = (e) => {
      this.debugLog("Received event from bus", e.detail);
      cb(e.detail);
    };
    __privateGet(this, _eventTarget).call(this).addEventListener(eventName, handler);
    this.debugLog("Registered event to bus", eventName);
    return () => {
      var _a2;
      if (withEventTarget) (_a2 = __privateGet(this, _internalEventTarget)) == null ? void 0 : _a2.removeEventListener(eventName, handler);
      __privateGet(this, _eventTarget).call(this).removeEventListener(eventName, handler);
    };
  }
  onAll(cb) {
    if (!__privateGet(this, _enabled)) {
      this.debugLog("Event bus client is disabled, not registering event");
      return () => {
      };
    }
    const handler = (e) => {
      const event = e.detail;
      cb(event);
    };
    __privateGet(this, _eventTarget).call(this).addEventListener("tanstack-devtools-global", handler);
    return () => __privateGet(this, _eventTarget).call(this).removeEventListener("tanstack-devtools-global", handler);
  }
  onAllPluginEvents(cb) {
    if (!__privateGet(this, _enabled)) {
      this.debugLog("Event bus client is disabled, not registering event");
      return () => {
      };
    }
    const handler = (e) => {
      const event = e.detail;
      if (__privateGet(this, _pluginId) && event.pluginId !== __privateGet(this, _pluginId)) return;
      cb(event);
    };
    __privateGet(this, _eventTarget).call(this).addEventListener("tanstack-devtools-global", handler);
    return () => __privateGet(this, _eventTarget).call(this).removeEventListener("tanstack-devtools-global", handler);
  }
}, _enabled = new WeakMap(), _pluginId = new WeakMap(), _eventTarget = new WeakMap(), _debug = new WeakMap(), _queuedEvents = new WeakMap(), _connected = new WeakMap(), _connectIntervalId = new WeakMap(), _connectEveryMs = new WeakMap(), _retryCount = new WeakMap(), _maxRetries = new WeakMap(), _connecting = new WeakMap(), _failedToConnect = new WeakMap(), _internalEventTarget = new WeakMap(), _onConnected = new WeakMap(), _retryConnection = new WeakMap(), _connectFunction = new WeakMap(), _a);

// node_modules/@tanstack/form-core/dist/esm/EventClient.js
var FormEventClient = class extends EventClient {
  constructor() {
    super({
      pluginId: "form-devtools",
      reconnectEveryMs: 1e3
    });
  }
};
var formEventClient = new FormEventClient();

// node_modules/@tanstack/form-core/dist/esm/utils.js
function functionalUpdate(updater, input) {
  return typeof updater === "function" ? updater(input) : updater;
}
function getBy(obj, path) {
  const pathObj = makePathArray(path);
  return pathObj.reduce((current, pathPart) => {
    if (current === null) return null;
    if (typeof current !== "undefined") {
      return current[pathPart];
    }
    return void 0;
  }, obj);
}
function setBy(obj, _path, updater) {
  const path = makePathArray(_path);
  function doSet(parent) {
    if (!path.length) {
      return functionalUpdate(updater, parent);
    }
    const key = path.shift();
    if (typeof key === "string" || typeof key === "number" && !Array.isArray(parent)) {
      if (typeof parent === "object") {
        if (parent === null) {
          parent = {};
        }
        return {
          ...parent,
          [key]: doSet(parent[key])
        };
      }
      return {
        [key]: doSet()
      };
    }
    if (Array.isArray(parent) && typeof key === "number") {
      const prefix = parent.slice(0, key);
      return [
        ...prefix.length ? prefix : new Array(key),
        doSet(parent[key]),
        ...parent.slice(key + 1)
      ];
    }
    return [...new Array(key), doSet()];
  }
  return doSet(obj);
}
function deleteBy(obj, _path) {
  const path = makePathArray(_path);
  function doDelete(parent) {
    if (!parent) return;
    if (path.length === 1) {
      const finalPath = path[0];
      if (Array.isArray(parent) && typeof finalPath === "number") {
        return parent.filter((_, i) => i !== finalPath);
      }
      const { [finalPath]: remove, ...rest } = parent;
      return rest;
    }
    const key = path.shift();
    if (typeof key === "string" || typeof key === "number" && !Array.isArray(parent)) {
      if (typeof parent === "object") {
        return {
          ...parent,
          [key]: doDelete(parent[key])
        };
      }
    }
    if (typeof key === "number") {
      if (Array.isArray(parent)) {
        if (key >= parent.length) {
          return parent;
        }
        const prefix = parent.slice(0, key);
        return [
          ...prefix.length ? prefix : new Array(key),
          doDelete(parent[key]),
          ...parent.slice(key + 1)
        ];
      }
    }
    throw new Error("It seems we have created an infinite loop in deleteBy. ");
  }
  return doDelete(obj);
}
var CC_DOT = 46;
var CC_OPEN = 91;
var CC_CLOSE = 93;
var CC_ZERO = 48;
var CC_NINE = 57;
function makePathArray(str) {
  if (Array.isArray(str)) {
    return [...str];
  }
  if (typeof str !== "string") {
    throw new Error("Path must be a string.");
  }
  const len = str.length;
  const result = [];
  let segStart = len > 0 && str.charCodeAt(0) === CC_OPEN ? 1 : 0;
  let allDigits = true;
  let prev = -1;
  for (let i = segStart; i <= len; i++) {
    const char = i < len ? str.charCodeAt(i) : -1;
    if (i === len || char === CC_DOT || char === CC_OPEN || char === CC_CLOSE) {
      const segLen = i - segStart;
      if (segLen > 0) {
        const treatAsNumber = (
          // ...it must contain only digits...
          allDigits && // ...and either be a single '0' or not start with '0'.
          (segLen === 1 || str.charCodeAt(segStart) !== CC_ZERO)
        );
        const seg = str.slice(segStart, i);
        if (treatAsNumber) {
          const num = parseInt(seg, 10);
          if (segLen <= 15 || String(num) === seg) {
            result.push(num);
          } else {
            result.push(seg);
          }
        } else {
          result.push(seg);
        }
      } else if (
        // This branch, which handles empty segments, only exists to preserve
        // the old behavior for malformed input.
        // Push the empty segment unless this is a "phantom boundary" the
        // old regex impl would have absorbed:
        //   1. `]` was always stripped — `prev === ']'` means the real
        //      boundary already happened on the previous iteration.
        //   2. A leading `]` was stripped too (the leading `[` strip
        //      above handles its counterpart for `[`).
        //   3. `..` and `[[` collapse to a single boundary.
        prev !== CC_CLOSE && !(prev === -1 && char === CC_CLOSE) && !(prev === char && (char === CC_DOT || char === CC_OPEN))
      ) {
        result.push("");
      }
      segStart = i + 1;
      allDigits = true;
    } else if (char < CC_ZERO || char > CC_NINE) {
      allDigits = false;
    }
    prev = char;
  }
  if (!result.length) result.push("");
  return result;
}
function concatenatePaths(path1, path2) {
  if (path1.length === 0) return path2;
  if (path2.length === 0) return path1;
  if (path2.startsWith("[")) {
    return path1 + path2;
  }
  if (path2.startsWith(".")) {
    return path1 + path2;
  }
  return `${path1}.${path2}`;
}
function isNonEmptyArray(obj) {
  return !(Array.isArray(obj) && obj.length === 0);
}
function getSyncValidatorArray(cause, options) {
  const runValidation = (props) => {
    return props.validators.filter(Boolean).map((validator) => {
      return {
        cause: validator.cause,
        validate: validator.fn
      };
    });
  };
  return options.validationLogic({
    form: options.form,
    group: options.group,
    validators: options.validators,
    event: { type: cause, fieldName: options.fieldName, async: false },
    runValidation
  });
}
function getAsyncValidatorArray(cause, options) {
  const { asyncDebounceMs } = options;
  const {
    onBlurAsyncDebounceMs,
    onChangeAsyncDebounceMs,
    onDynamicAsyncDebounceMs
  } = options.validators || {};
  const defaultDebounceMs = asyncDebounceMs ?? 0;
  const runValidation = (props) => {
    return props.validators.filter(Boolean).map((validator) => {
      const validatorCause = (validator == null ? void 0 : validator.cause) || cause;
      let debounceMs = defaultDebounceMs;
      switch (validatorCause) {
        case "change":
          debounceMs = onChangeAsyncDebounceMs ?? defaultDebounceMs;
          break;
        case "blur":
          debounceMs = onBlurAsyncDebounceMs ?? defaultDebounceMs;
          break;
        case "dynamic":
          debounceMs = onDynamicAsyncDebounceMs ?? defaultDebounceMs;
          break;
        case "submit":
          debounceMs = 0;
          break;
      }
      if (cause === "submit") {
        debounceMs = 0;
      }
      return {
        cause: validatorCause,
        validate: validator.fn,
        debounceMs
      };
    });
  };
  return options.validationLogic({
    form: options.form,
    group: options.group,
    validators: options.validators,
    event: { type: cause, fieldName: options.fieldName, async: true },
    runValidation
  });
}
var isGlobalFormValidationError = (error) => {
  return !!error && typeof error === "object" && "fields" in error;
};
function evaluate(objA, objB) {
  if (Object.is(objA, objB)) {
    return true;
  }
  if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
    return false;
  }
  if (objA instanceof Date && objB instanceof Date) {
    return objA.getTime() === objB.getTime();
  }
  if (objA instanceof Map && objB instanceof Map) {
    if (objA.size !== objB.size) return false;
    for (const [k, v] of objA) {
      if (!objB.has(k) || !Object.is(v, objB.get(k))) return false;
    }
    return true;
  }
  if (objA instanceof Set && objB instanceof Set) {
    if (objA.size !== objB.size) return false;
    for (const v of objA) {
      if (!objB.has(v)) return false;
    }
    return true;
  }
  const keysA = Object.keys(objA);
  const keysB = Object.keys(objB);
  if (keysA.length !== keysB.length) {
    return false;
  }
  if (keysA.length === 0 && !Array.isArray(objA) && !Array.isArray(objB) && (Object.getPrototypeOf(objA) !== Object.prototype || Object.getPrototypeOf(objB) !== Object.prototype)) {
    return false;
  }
  for (const key of keysA) {
    if (!keysB.includes(key) || !evaluate(objA[key], objB[key])) {
      return false;
    }
  }
  return true;
}
var determineFormLevelErrorSourceAndValue = ({
  newFormValidatorError,
  isPreviousErrorFromFormValidator,
  previousErrorValue
}) => {
  if (newFormValidatorError) {
    return { newErrorValue: newFormValidatorError, newSource: "form" };
  }
  if (isPreviousErrorFromFormValidator) {
    return { newErrorValue: void 0, newSource: void 0 };
  }
  if (previousErrorValue) {
    return { newErrorValue: previousErrorValue, newSource: "field" };
  }
  return { newErrorValue: void 0, newSource: void 0 };
};
var determineFieldLevelErrorSourceAndValue = ({
  formLevelError,
  fieldLevelError
}) => {
  if (fieldLevelError) {
    return { newErrorValue: fieldLevelError, newSource: "field" };
  }
  if (formLevelError) {
    return { newErrorValue: formLevelError, newSource: "form" };
  }
  return { newErrorValue: void 0, newSource: void 0 };
};
function createFieldMap(values) {
  const output = {};
  for (const key in values) {
    output[key] = key;
  }
  return output;
}
function mergeOpts(originalOpts, overrides) {
  if (originalOpts === void 0 || originalOpts === null) {
    return overrides;
  }
  return { ...originalOpts, ...overrides };
}
var IDX = 256;
var HEX = [];
var BUFFER;
while (IDX--) {
  HEX[IDX] = (IDX + 256).toString(16).substring(1);
}
function uuid() {
  let i = 0;
  let num;
  let out = "";
  if (!BUFFER || IDX + 16 > 256) {
    BUFFER = new Array(256);
    i = 256;
    while (i--) {
      BUFFER[i] = 256 * Math.random() | 0;
    }
    i = 0;
    IDX = 0;
  }
  for (; i < 16; i++) {
    num = BUFFER[IDX + i];
    if (i === 6) out += HEX[num & 15 | 64];
    else if (i === 8) out += HEX[num & 63 | 128];
    else out += HEX[num];
    if (i & 1 && i > 1 && i < 11) out += "-";
  }
  IDX++;
  return out;
}
var throttleFormState = liteThrottle(
  (form) => formEventClient.emit("form-state", {
    id: form.formId,
    state: form.store.state
  }),
  {
    wait: 300
  }
);
function deepCopy(obj) {
  if (obj === null || typeof obj !== "object") {
    return obj;
  }
  if (obj instanceof Date) {
    return new Date(obj.getTime());
  }
  if (Array.isArray(obj)) {
    const arrCopy = [];
    for (let i = 0; i < obj.length; i++) {
      arrCopy[i] = deepCopy(obj[i]);
    }
    return arrCopy;
  }
  if (obj instanceof Map) {
    const mapCopy = /* @__PURE__ */ new Map();
    obj.forEach((value, key) => {
      mapCopy.set(key, deepCopy(value));
    });
    return mapCopy;
  }
  if (obj instanceof Set) {
    const setCopy = /* @__PURE__ */ new Set();
    obj.forEach((value) => {
      setCopy.add(deepCopy(value));
    });
    return setCopy;
  }
  const copy = {};
  for (const key in obj) {
    if (Object.prototype.hasOwnProperty.call(obj, key)) {
      copy[key] = deepCopy(obj[key]);
    }
  }
  return copy;
}
function isFieldInGroup(groupName, fieldName) {
  return fieldName === groupName || fieldName.startsWith(`${groupName}.`) || fieldName.startsWith(`${groupName}[`);
}

// node_modules/@tanstack/form-core/dist/esm/ValidationLogic.js
var revalidateLogic = ({
  mode = "submit",
  modeAfterSubmission = "change"
} = {}) => (props) => {
  const validatorNames = Object.keys(props.validators ?? {});
  if (validatorNames.length === 0) {
    return props.runValidation({
      validators: [],
      form: props.form
    });
  }
  const dynamicValidator = {
    fn: props.event.async ? props.validators["onDynamicAsync"] : props.validators["onDynamic"],
    cause: "dynamic"
  };
  const validatorsToAdd = [];
  const submissionAttempts = props.group ? props.group.state.meta.submissionAttempts : props.form.state.submissionAttempts;
  const modeToWatch = submissionAttempts === 0 ? mode : modeAfterSubmission;
  if ([modeToWatch, "submit"].includes(props.event.type)) {
    validatorsToAdd.push(dynamicValidator);
  }
  let defaultValidators = [];
  defaultValidationLogic({
    ...props,
    runValidation: (vProps) => {
      defaultValidators = vProps.validators;
    }
  });
  if (validatorsToAdd.length === 0) {
    return props.runValidation({
      validators: defaultValidators,
      form: props.form
    });
  }
  return props.runValidation({
    validators: [...defaultValidators, ...validatorsToAdd],
    form: props.form
  });
};
var defaultValidationLogic = (props) => {
  if (!props.validators) {
    return props.runValidation({
      validators: [],
      form: props.form
    });
  }
  const isAsync = props.event.async;
  const onMountValidator = isAsync ? void 0 : { fn: props.validators.onMount, cause: "mount" };
  const onChangeValidator = {
    fn: isAsync ? props.validators.onChangeAsync : props.validators.onChange,
    cause: "change"
  };
  const onBlurValidator = {
    fn: isAsync ? props.validators.onBlurAsync : props.validators.onBlur,
    cause: "blur"
  };
  const onSubmitValidator = {
    fn: isAsync ? props.validators.onSubmitAsync : props.validators.onSubmit,
    cause: "submit"
  };
  const onServerValidator = isAsync ? void 0 : { fn: () => void 0, cause: "server" };
  switch (props.event.type) {
    case "mount": {
      return props.runValidation({
        validators: [onMountValidator],
        form: props.form
      });
    }
    case "submit": {
      return props.runValidation({
        validators: [
          onChangeValidator,
          onBlurValidator,
          onSubmitValidator,
          onServerValidator
        ],
        form: props.form
      });
    }
    case "server": {
      return props.runValidation({
        validators: [],
        form: props.form
      });
    }
    case "blur": {
      return props.runValidation({
        validators: [onBlurValidator, onServerValidator],
        form: props.form
      });
    }
    case "change": {
      return props.runValidation({
        validators: [onChangeValidator, onServerValidator],
        form: props.form
      });
    }
    default: {
      throw new Error(`Unknown validation event type: ${props.event.type}`);
    }
  }
};

// node_modules/@tanstack/form-core/dist/esm/standardSchemaValidator.js
function prefixSchemaToErrors(issues, formValue) {
  const schema = /* @__PURE__ */ new Map();
  for (const issue of issues) {
    const issuePath = issue.path ?? [];
    let currentFormValue = formValue;
    let path = "";
    for (let i = 0; i < issuePath.length; i++) {
      const pathSegment = issuePath[i];
      if (pathSegment === void 0) continue;
      const segment = typeof pathSegment === "object" ? pathSegment.key : pathSegment;
      const segmentAsNumber = Number(segment);
      if (Array.isArray(currentFormValue) && !Number.isNaN(segmentAsNumber)) {
        path += `[${segmentAsNumber}]`;
      } else {
        path += (i > 0 ? "." : "") + String(segment);
      }
      if (typeof currentFormValue === "object" && currentFormValue !== null) {
        currentFormValue = currentFormValue[segment];
      } else {
        currentFormValue = void 0;
      }
    }
    schema.set(path, (schema.get(path) ?? []).concat(issue));
  }
  return Object.fromEntries(schema);
}
var transformFormIssues = (issues, formValue) => {
  const schemaErrors = prefixSchemaToErrors(issues, formValue);
  return {
    form: schemaErrors,
    fields: schemaErrors
  };
};
var standardSchemaValidators = {
  validate({
    value,
    validationSource
  }, schema) {
    const result = schema["~standard"].validate(value);
    if (result instanceof Promise) {
      throw new Error("async function passed to sync validator");
    }
    if (!result.issues) return;
    if (validationSource === "field") {
      return result.issues;
    }
    return transformFormIssues(result.issues, value);
  },
  async validateAsync({
    value,
    validationSource
  }, schema) {
    const result = await schema["~standard"].validate(value);
    if (!result.issues) return;
    if (validationSource === "field") {
      return result.issues;
    }
    return transformFormIssues(result.issues, value);
  }
};
var isStandardSchemaValidator = (validator) => !!validator && "~standard" in validator;

// node_modules/@tanstack/form-core/dist/esm/metaHelper.js
var defaultFieldMeta = {
  isValidating: false,
  isTouched: false,
  isBlurred: false,
  isDirty: false,
  isPristine: true,
  isValid: true,
  isDefaultValue: true,
  errors: [],
  errorMap: {},
  errorSourceMap: {},
  _arrayVersion: 0,
  _pendingValidationsCount: 0
};
function metaHelper(formApi) {
  function bumpArrayVersion(field) {
    const currentMeta = formApi.getFieldMeta(field) ?? defaultFieldMeta;
    formApi.setFieldMeta(field, {
      ...currentMeta,
      _arrayVersion: (currentMeta._arrayVersion || 0) + 1
    });
  }
  function handleArrayMove(field, fromIndex, toIndex) {
    bumpArrayVersion(field);
    const affectedFields = getAffectedFields(field, fromIndex, "move", toIndex);
    const startIndex = Math.min(fromIndex, toIndex);
    const endIndex = Math.max(fromIndex, toIndex);
    for (let i = startIndex; i <= endIndex; i++) {
      affectedFields.push(getFieldPath(field, i));
    }
    const fromFields = Object.keys(formApi.fieldInfo).reduce(
      (fieldMap, fieldKey) => {
        if (fieldKey.startsWith(getFieldPath(field, fromIndex))) {
          fieldMap.set(
            fieldKey,
            formApi.getFieldMeta(fieldKey)
          );
        }
        return fieldMap;
      },
      /* @__PURE__ */ new Map()
    );
    shiftMeta(affectedFields, fromIndex < toIndex ? "up" : "down");
    Object.keys(formApi.fieldInfo).filter((fieldKey) => fieldKey.startsWith(getFieldPath(field, toIndex))).forEach((fieldKey) => {
      const fromKey = fieldKey.replace(
        getFieldPath(field, toIndex),
        getFieldPath(field, fromIndex)
      );
      const fromMeta = fromFields.get(fromKey);
      if (fromMeta) {
        formApi.setFieldMeta(fieldKey, fromMeta);
      }
    });
  }
  function handleArrayRemove(field, index) {
    bumpArrayVersion(field);
    const affectedFields = getAffectedFields(field, index, "remove");
    shiftMeta(affectedFields, "up");
  }
  function handleArraySwap(field, index, secondIndex) {
    bumpArrayVersion(field);
    const affectedFields = getAffectedFields(field, index, "swap", secondIndex);
    affectedFields.forEach((fieldKey) => {
      if (!fieldKey.toString().startsWith(getFieldPath(field, index))) {
        return;
      }
      const swappedKey = fieldKey.toString().replace(
        getFieldPath(field, index),
        getFieldPath(field, secondIndex)
      );
      const [meta1, meta2] = [
        formApi.getFieldMeta(fieldKey),
        formApi.getFieldMeta(swappedKey)
      ];
      if (meta1) formApi.setFieldMeta(swappedKey, meta1);
      if (meta2) formApi.setFieldMeta(fieldKey, meta2);
    });
  }
  function handleArrayInsert(field, insertIndex) {
    bumpArrayVersion(field);
    const affectedFields = getAffectedFields(field, insertIndex, "insert");
    shiftMeta(affectedFields, "down");
    affectedFields.forEach((fieldKey) => {
      if (fieldKey.toString().startsWith(getFieldPath(field, insertIndex))) {
        formApi.setFieldMeta(fieldKey, getEmptyFieldMeta());
      }
    });
  }
  function getFieldPath(field, index) {
    return `${field}[${index}]`;
  }
  function getAffectedFields(field, index, mode, secondIndex) {
    const affectedFieldKeys = [getFieldPath(field, index)];
    switch (mode) {
      case "swap":
        affectedFieldKeys.push(getFieldPath(field, secondIndex));
        break;
      case "move": {
        const [startIndex, endIndex] = [
          Math.min(index, secondIndex),
          Math.max(index, secondIndex)
        ];
        for (let i = startIndex; i <= endIndex; i++) {
          affectedFieldKeys.push(getFieldPath(field, i));
        }
        break;
      }
      default: {
        const currentValue = formApi.getFieldValue(field);
        const fieldItems = Array.isArray(currentValue) ? currentValue.length : 0;
        for (let i = index + 1; i < fieldItems; i++) {
          affectedFieldKeys.push(getFieldPath(field, i));
        }
        break;
      }
    }
    return Object.keys(formApi.fieldInfo).filter(
      (fieldKey) => affectedFieldKeys.some((key) => fieldKey.startsWith(key))
    );
  }
  function updateIndex(fieldKey, direction) {
    return fieldKey.replace(/\[(\d+)\]/, (_, num) => {
      const currIndex = parseInt(num, 10);
      const newIndex = direction === "up" ? currIndex + 1 : Math.max(0, currIndex - 1);
      return `[${newIndex}]`;
    });
  }
  function shiftMeta(fields, direction) {
    const sortedFields = direction === "up" ? fields : [...fields].reverse();
    sortedFields.forEach((fieldKey) => {
      const nextFieldKey = updateIndex(fieldKey.toString(), direction);
      const nextFieldMeta = formApi.getFieldMeta(nextFieldKey);
      if (nextFieldMeta) {
        formApi.setFieldMeta(fieldKey, nextFieldMeta);
      } else {
        formApi.setFieldMeta(fieldKey, getEmptyFieldMeta());
      }
    });
  }
  const getEmptyFieldMeta = () => defaultFieldMeta;
  return {
    bumpArrayVersion,
    handleArrayMove,
    handleArrayRemove,
    handleArraySwap,
    handleArrayInsert
  };
}

// node_modules/@tanstack/form-core/dist/esm/FormApi.js
function getDefaultFormState(defaultState) {
  return {
    values: defaultState.values ?? {},
    errorMap: defaultState.errorMap ?? {},
    fieldMetaBase: defaultState.fieldMetaBase ?? {},
    formGroupStateBase: defaultState.formGroupStateBase ?? {},
    isSubmitted: defaultState.isSubmitted ?? false,
    isSubmitting: defaultState.isSubmitting ?? false,
    isValidating: defaultState.isValidating ?? false,
    submissionAttempts: defaultState.submissionAttempts ?? 0,
    isSubmitSuccessful: defaultState.isSubmitSuccessful ?? false,
    validationMetaMap: defaultState.validationMetaMap ?? {
      onChange: void 0,
      onBlur: void 0,
      onSubmit: void 0,
      onMount: void 0,
      onServer: void 0,
      onDynamic: void 0
    }
  };
}
var FormApi = class {
  /**
   * Constructs a new `FormApi` instance with the given form options.
   */
  constructor(opts) {
    var _a2;
    this.options = {};
    this.fieldInfo = {};
    this.formGroupApis = /* @__PURE__ */ new Set();
    this.mount = () => {
      var _a3, _b;
      const cleanupDevtoolBroadcast = this.store.subscribe(() => {
        throttleFormState(this);
      });
      const cleanupFormStateListener = formEventClient.on(
        "request-form-state",
        (e) => {
          if (e.payload.id === this._formId) {
            formEventClient.emit("form-api", {
              id: this._formId,
              state: this.store.state,
              options: this.options
            });
          }
        }
      );
      const cleanupFormResetListener = formEventClient.on(
        "request-form-reset",
        (e) => {
          if (e.payload.id === this._formId) {
            this.reset();
          }
        }
      );
      const cleanupFormForceSubmitListener = formEventClient.on(
        "request-form-force-submit",
        (e) => {
          if (e.payload.id === this._formId) {
            this._devtoolsSubmissionOverride = true;
            this.handleSubmit();
            this._devtoolsSubmissionOverride = false;
          }
        }
      );
      const cleanup = () => {
        cleanupFormForceSubmitListener();
        cleanupFormResetListener();
        cleanupFormStateListener();
        cleanupDevtoolBroadcast.unsubscribe();
        formEventClient.emit("form-unmounted", {
          id: this._formId
        });
      };
      (_b = (_a3 = this.options.listeners) == null ? void 0 : _a3.onMount) == null ? void 0 : _b.call(_a3, { formApi: this });
      const { onMount } = this.options.validators || {};
      formEventClient.emit("form-api", {
        id: this._formId,
        state: this.store.state,
        options: this.options
      });
      if (!onMount) return cleanup;
      this.validateSync("mount");
      return cleanup;
    };
    this.update = (options) => {
      if (!options) return;
      const oldOptions = this.options;
      this.options = options;
      const shouldUpdateValues = options.defaultValues && !evaluate(options.defaultValues, oldOptions.defaultValues) && !this.state.isTouched;
      const shouldUpdateState = !evaluate(options.defaultState, oldOptions.defaultState) && !this.state.isTouched;
      if (!shouldUpdateValues && !shouldUpdateState) return;
      batch(() => {
        this.baseStore.setState(
          () => getDefaultFormState(
            Object.assign(
              {},
              this.state,
              shouldUpdateState ? options.defaultState : {},
              shouldUpdateValues ? {
                values: options.defaultValues
              } : {}
            )
          )
        );
      });
      if (shouldUpdateValues) {
        const helper = metaHelper(this);
        for (const fieldKey of Object.keys(
          this.fieldInfo
        )) {
          if (Array.isArray(this.getFieldValue(fieldKey))) {
            helper.bumpArrayVersion(fieldKey);
          }
        }
      }
      formEventClient.emit("form-api", {
        id: this._formId,
        state: this.store.state,
        options: this.options
      });
    };
    this.reset = (values, opts2) => {
      const { fieldMeta: currentFieldMeta } = this.state;
      const fieldMetaBase = this.resetFieldMeta(currentFieldMeta);
      if (values && !(opts2 == null ? void 0 : opts2.keepDefaultValues)) {
        this.options = {
          ...this.options,
          defaultValues: values
        };
      }
      this.baseStore.setState(() => {
        var _a3;
        let nextValues = values ?? this.options.defaultValues ?? ((_a3 = this.options.defaultState) == null ? void 0 : _a3.values);
        if (!values) {
          Object.values(this.fieldInfo).forEach(
            (fieldInfo) => {
              if (fieldInfo.instance && fieldInfo.instance.options.defaultValue !== void 0) {
                nextValues = setBy(
                  nextValues,
                  fieldInfo.instance.name,
                  fieldInfo.instance.options.defaultValue
                );
              }
            }
          );
        }
        return getDefaultFormState({
          ...this.options.defaultState,
          values: nextValues,
          fieldMetaBase
        });
      });
    };
    this.validateAllFields = async (cause) => {
      const fieldValidationPromises = [];
      batch(() => {
        void Object.values(this.fieldInfo).forEach(
          (field) => {
            if (!field.instance) return;
            const fieldInstance = field.instance;
            fieldValidationPromises.push(
              // Remember, `validate` is either a sync operation or a promise
              Promise.resolve().then(
                () => fieldInstance.validate(cause, {
                  skipFormValidation: true,
                  skipGroupValidation: true
                })
              )
            );
            if (!field.instance.store.state.meta.isTouched) {
              field.instance.setMeta((prev) => ({ ...prev, isTouched: true }));
            }
          }
        );
      });
      const fieldErrorMapMap = await Promise.all(fieldValidationPromises);
      return fieldErrorMapMap.flat();
    };
    this.validateArrayFieldsStartingFrom = async (field, index, cause) => {
      const currentValue = this.getFieldValue(field);
      const lastIndex = Array.isArray(currentValue) ? Math.max(currentValue.length - 1, 0) : null;
      const fieldKeysToValidate = [`${field}[${index}]`];
      for (let i = index + 1; i <= (lastIndex ?? 0); i++) {
        fieldKeysToValidate.push(`${field}[${i}]`);
      }
      const fieldsToValidate = Object.keys(this.fieldInfo).filter(
        (fieldKey) => fieldKeysToValidate.some((key) => fieldKey.startsWith(key))
      );
      const fieldValidationPromises = [];
      batch(() => {
        fieldsToValidate.forEach((nestedField) => {
          fieldValidationPromises.push(
            Promise.resolve().then(() => this.validateField(nestedField, cause))
          );
        });
      });
      const fieldErrorMapMap = await Promise.all(fieldValidationPromises);
      return fieldErrorMapMap.flat();
    };
    this.validateField = (field, cause) => {
      var _a3, _b;
      const fieldInstance = (_a3 = this.fieldInfo[field]) == null ? void 0 : _a3.instance;
      if (!fieldInstance) {
        const { hasErrored } = this.validateSync(cause);
        if (hasErrored && !this.options.asyncAlways) {
          return ((_b = this.getFieldMeta(field)) == null ? void 0 : _b.errors) ?? [];
        }
        return this.validateAsync(cause).then(() => {
          var _a4;
          return ((_a4 = this.getFieldMeta(field)) == null ? void 0 : _a4.errors) ?? [];
        });
      }
      if (!fieldInstance.store.state.meta.isTouched) {
        fieldInstance.setMeta((prev) => ({ ...prev, isTouched: true }));
      }
      return fieldInstance.validate(cause);
    };
    this.validateSync = (cause, validateOpts) => {
      const validates = getSyncValidatorArray(cause, {
        ...this.options,
        form: this,
        group: validateOpts == null ? void 0 : validateOpts.group,
        validationLogic: this.options.validationLogic || defaultValidationLogic
      });
      let hasErrored = false;
      const currentValidationErrorMap = {};
      batch(() => {
        var _a3, _b, _c;
        for (const validateObj of validates) {
          if (!validateObj.validate) continue;
          const rawError = this.runValidator({
            validate: validateObj.validate,
            value: {
              value: this.state.values,
              formApi: this,
              validationSource: "form"
            },
            type: "validate"
          });
          const { formError, fieldErrors } = normalizeError(rawError);
          const errorMapKey = getErrorMapKey(validateObj.cause);
          let allFieldsToProcess = /* @__PURE__ */ new Set([
            ...Object.keys(this.state.fieldMeta),
            ...Object.keys(fieldErrors || {})
          ]);
          if (validateOpts == null ? void 0 : validateOpts.filterFieldNames) {
            allFieldsToProcess = new Set(
              [...allFieldsToProcess].filter(validateOpts.filterFieldNames)
            );
          }
          for (const field of allFieldsToProcess) {
            if (this.baseStore.state.fieldMetaBase[field] === void 0 && !(fieldErrors == null ? void 0 : fieldErrors[field])) {
              continue;
            }
            const fieldMeta = this.getFieldMeta(field) ?? defaultFieldMeta;
            const {
              errorMap: currentErrorMap,
              errorSourceMap: currentErrorMapSource
            } = fieldMeta;
            const newFormValidatorError = fieldErrors == null ? void 0 : fieldErrors[field];
            const { newErrorValue, newSource } = determineFormLevelErrorSourceAndValue({
              newFormValidatorError,
              isPreviousErrorFromFormValidator: (
                // These conditional checks are required, otherwise we get runtime errors.
                // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                (currentErrorMapSource == null ? void 0 : currentErrorMapSource[errorMapKey]) === "form"
              ),
              // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
              previousErrorValue: currentErrorMap == null ? void 0 : currentErrorMap[errorMapKey]
            });
            if (newSource === "form") {
              currentValidationErrorMap[field] = {
                ...currentValidationErrorMap[field],
                [errorMapKey]: newFormValidatorError
              };
            }
            if ((currentErrorMap == null ? void 0 : currentErrorMap[errorMapKey]) !== newErrorValue) {
              this.setFieldMeta(field, (prev = defaultFieldMeta) => ({
                ...prev,
                errorMap: {
                  ...prev.errorMap,
                  [errorMapKey]: newErrorValue
                },
                errorSourceMap: {
                  ...prev.errorSourceMap,
                  [errorMapKey]: newSource
                }
              }));
            }
          }
          if (!(validateOpts == null ? void 0 : validateOpts.dontUpdateFormErrorMap)) {
            if (((_a3 = this.state.errorMap) == null ? void 0 : _a3[errorMapKey]) !== formError) {
              this.baseStore.setState((prev) => ({
                ...prev,
                errorMap: {
                  ...prev.errorMap,
                  [errorMapKey]: formError
                }
              }));
            }
          }
          if (formError || fieldErrors) {
            hasErrored = true;
          }
        }
        if (validateOpts == null ? void 0 : validateOpts.dontUpdateFormErrorMap) {
          return;
        }
        const submitErrKey = getErrorMapKey("submit");
        if (
          // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
          ((_b = this.state.errorMap) == null ? void 0 : _b[submitErrKey]) && cause !== "submit" && !hasErrored
        ) {
          this.baseStore.setState((prev) => ({
            ...prev,
            errorMap: {
              ...prev.errorMap,
              [submitErrKey]: void 0
            }
          }));
        }
        const serverErrKey = getErrorMapKey("server");
        if (
          // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
          ((_c = this.state.errorMap) == null ? void 0 : _c[serverErrKey]) && cause !== "server" && !hasErrored
        ) {
          this.baseStore.setState((prev) => ({
            ...prev,
            errorMap: {
              ...prev.errorMap,
              [serverErrKey]: void 0
            }
          }));
        }
      });
      return { hasErrored, fieldsErrorMap: currentValidationErrorMap };
    };
    this.validateAsync = async (cause, validateOpts) => {
      const validates = getAsyncValidatorArray(cause, {
        ...this.options,
        form: this,
        group: validateOpts == null ? void 0 : validateOpts.group,
        validationLogic: this.options.validationLogic || defaultValidationLogic
      });
      if (!this.state.isFormValidating) {
        this.baseStore.setState((prev) => ({ ...prev, isFormValidating: true }));
      }
      const promises = [];
      let fieldErrorsFromFormValidators;
      for (const validateObj of validates) {
        if (!validateObj.validate) continue;
        const key = getErrorMapKey(validateObj.cause);
        const fieldValidatorMeta = this.state.validationMetaMap[key];
        fieldValidatorMeta == null ? void 0 : fieldValidatorMeta.lastAbortController.abort();
        const controller = new AbortController();
        this.state.validationMetaMap[key] = {
          lastAbortController: controller
        };
        promises.push(
          new Promise(async (resolve) => {
            let rawError;
            try {
              rawError = await new Promise((rawResolve, rawReject) => {
                setTimeout(async () => {
                  if (controller.signal.aborted) return rawResolve(void 0);
                  try {
                    rawResolve(
                      await this.runValidator({
                        validate: validateObj.validate,
                        value: {
                          value: this.state.values,
                          formApi: this,
                          validationSource: "form",
                          signal: controller.signal
                        },
                        type: "validateAsync"
                      })
                    );
                  } catch (e) {
                    rawReject(e);
                  }
                }, validateObj.debounceMs);
              });
            } catch (e) {
              rawError = e;
            }
            const { formError, fieldErrors: fieldErrorsFromNormalizeError } = normalizeError(rawError);
            if (fieldErrorsFromNormalizeError) {
              fieldErrorsFromFormValidators = fieldErrorsFromFormValidators ? {
                ...fieldErrorsFromFormValidators,
                ...fieldErrorsFromNormalizeError
              } : fieldErrorsFromNormalizeError;
            }
            const errorMapKey = getErrorMapKey(validateObj.cause);
            const allFieldsToProcess = /* @__PURE__ */ new Set([
              ...Object.keys(this.state.fieldMeta),
              ...Object.keys(fieldErrorsFromFormValidators || {})
            ]);
            let fields = Array.from(allFieldsToProcess);
            if (validateOpts == null ? void 0 : validateOpts.filterFieldNames) {
              fields = fields.filter(validateOpts.filterFieldNames);
            }
            for (const field of fields) {
              if (this.baseStore.state.fieldMetaBase[field] === void 0 && !(fieldErrorsFromFormValidators == null ? void 0 : fieldErrorsFromFormValidators[field])) {
                continue;
              }
              const fieldMeta = this.getFieldMeta(field) ?? defaultFieldMeta;
              const {
                errorMap: currentErrorMap,
                errorSourceMap: currentErrorMapSource
              } = fieldMeta;
              const newFormValidatorError = fieldErrorsFromFormValidators == null ? void 0 : fieldErrorsFromFormValidators[field];
              const { newErrorValue, newSource } = determineFormLevelErrorSourceAndValue({
                newFormValidatorError,
                isPreviousErrorFromFormValidator: (
                  // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                  (currentErrorMapSource == null ? void 0 : currentErrorMapSource[errorMapKey]) === "form"
                ),
                // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                previousErrorValue: currentErrorMap == null ? void 0 : currentErrorMap[errorMapKey]
              });
              if ((currentErrorMap == null ? void 0 : currentErrorMap[errorMapKey]) !== newErrorValue) {
                this.setFieldMeta(field, (prev = defaultFieldMeta) => ({
                  ...prev,
                  errorMap: {
                    ...prev.errorMap,
                    [errorMapKey]: newErrorValue
                  },
                  errorSourceMap: {
                    ...prev.errorSourceMap,
                    [errorMapKey]: newSource
                  }
                }));
              }
            }
            if (!(validateOpts == null ? void 0 : validateOpts.dontUpdateFormErrorMap)) {
              this.baseStore.setState((prev) => ({
                ...prev,
                errorMap: {
                  ...prev.errorMap,
                  [errorMapKey]: formError
                }
              }));
            }
            resolve(
              fieldErrorsFromFormValidators ? { fieldErrors: fieldErrorsFromFormValidators, errorMapKey } : void 0
            );
          })
        );
      }
      let results = [];
      const fieldsErrorMap = {};
      if (promises.length) {
        results = await Promise.all(promises);
        for (const fieldValidationResult of results) {
          if (fieldValidationResult == null ? void 0 : fieldValidationResult.fieldErrors) {
            const { errorMapKey } = fieldValidationResult;
            for (const [field, fieldError] of Object.entries(
              fieldValidationResult.fieldErrors
            )) {
              const oldErrorMap = fieldsErrorMap[field] || {};
              const newErrorMap = {
                ...oldErrorMap,
                [errorMapKey]: fieldError
              };
              fieldsErrorMap[field] = newErrorMap;
            }
          }
        }
      }
      this.baseStore.setState((prev) => ({
        ...prev,
        isFormValidating: false
      }));
      return fieldsErrorMap;
    };
    this.validate = (cause, validateOpts) => {
      const { hasErrored, fieldsErrorMap } = this.validateSync(
        cause,
        validateOpts
      );
      if (hasErrored && !this.options.asyncAlways) {
        return fieldsErrorMap;
      }
      return this.validateAsync(cause, validateOpts);
    };
    this._handleSubmit = async (submitMeta) => {
      var _a3, _b, _c, _d, _e, _f, _g, _h, _i, _j;
      this.baseStore.setState((old) => ({
        ...old,
        // Submission attempts mark the form as not submitted
        isSubmitted: false,
        // Count submission attempts
        submissionAttempts: old.submissionAttempts + 1,
        isSubmitSuccessful: false
        // Reset isSubmitSuccessful at the start of submission
      }));
      batch(() => {
        void Object.values(this.fieldInfo).forEach(
          (field) => {
            if (!field.instance) return;
            if (!field.instance.store.state.meta.isTouched) {
              field.instance.setMeta((prev) => ({ ...prev, isTouched: true }));
            }
          }
        );
      });
      const submitMetaArg = submitMeta ?? this.options.onSubmitMeta;
      if (!this.state.canSubmit && !this._devtoolsSubmissionOverride) {
        if (this.baseStore.state.submissionAttempts <= 1) {
          (_b = (_a3 = this.options).onSubmitInvalid) == null ? void 0 : _b.call(_a3, {
            value: this.state.values,
            formApi: this,
            meta: submitMetaArg
          });
          return;
        }
      }
      this.baseStore.setState((d) => ({ ...d, isSubmitting: true }));
      const done = () => {
        this.baseStore.setState((prev) => ({ ...prev, isSubmitting: false }));
      };
      await this.validateAllFields("submit");
      if (!this.state.isFieldsValid) {
        done();
        (_d = (_c = this.options).onSubmitInvalid) == null ? void 0 : _d.call(_c, {
          value: this.state.values,
          formApi: this,
          meta: submitMetaArg
        });
        formEventClient.emit("form-submission", {
          id: this._formId,
          submissionAttempt: this.state.submissionAttempts,
          successful: false,
          stage: "validateAllFields",
          errors: Object.values(this.state.fieldMeta).map((meta) => meta.errors).flat()
        });
        return;
      }
      await this.validate("submit");
      if (!this.state.isValid) {
        done();
        (_f = (_e = this.options).onSubmitInvalid) == null ? void 0 : _f.call(_e, {
          value: this.state.values,
          formApi: this,
          meta: submitMetaArg
        });
        formEventClient.emit("form-submission", {
          id: this._formId,
          submissionAttempt: this.state.submissionAttempts,
          successful: false,
          stage: "validate",
          errors: this.state.errors
        });
        return;
      }
      batch(() => {
        void Object.values(this.fieldInfo).forEach(
          (field) => {
            var _a4;
            (_a4 = field.instance) == null ? void 0 : _a4.triggerOnSubmitListener();
          }
        );
      });
      (_h = (_g = this.options.listeners) == null ? void 0 : _g.onSubmit) == null ? void 0 : _h.call(_g, { formApi: this, meta: submitMetaArg });
      try {
        await ((_j = (_i = this.options).onSubmit) == null ? void 0 : _j.call(_i, {
          value: this.state.values,
          formApi: this,
          meta: submitMetaArg
        }));
        batch(() => {
          this.baseStore.setState((prev) => ({
            ...prev,
            isSubmitted: true,
            isSubmitSuccessful: true
            // Set isSubmitSuccessful to true on successful submission
          }));
          formEventClient.emit("form-submission", {
            id: this._formId,
            submissionAttempt: this.state.submissionAttempts,
            successful: true
          });
          done();
        });
      } catch (err) {
        this.baseStore.setState((prev) => ({
          ...prev,
          isSubmitSuccessful: false
          // Ensure isSubmitSuccessful is false if an error occurs
        }));
        formEventClient.emit("form-submission", {
          id: this._formId,
          submissionAttempt: this.state.submissionAttempts,
          successful: false,
          stage: "inflight",
          onError: err
        });
        done();
        throw err;
      }
    };
    this.getFieldValue = (field) => getBy(this.state.values, field);
    this.getFieldMeta = (field) => {
      return this.state.fieldMeta[field];
    };
    this.getFormGroupMeta = (name) => {
      return this.formGroupMetaDerived.state[name];
    };
    this.getFieldInfo = (field) => {
      var _a3;
      return (_a3 = this.fieldInfo)[field] || (_a3[field] = {
        instance: null,
        validationMetaMap: {
          onChange: void 0,
          onBlur: void 0,
          onSubmit: void 0,
          onMount: void 0,
          onServer: void 0,
          onDynamic: void 0
        }
      });
    };
    this.setFieldMeta = (field, updater) => {
      this.baseStore.setState((prev) => {
        return {
          ...prev,
          fieldMetaBase: {
            ...prev.fieldMetaBase,
            [field]: functionalUpdate(
              updater,
              prev.fieldMetaBase[field]
            )
          }
        };
      });
    };
    this.resetFieldMeta = (fieldMeta) => {
      return Object.keys(fieldMeta).reduce(
        (acc, key) => {
          const fieldKey = key;
          acc[fieldKey] = defaultFieldMeta;
          return acc;
        },
        {}
      );
    };
    this.setFieldValue = (field, updater, opts2) => {
      var _a3;
      const dontUpdateMeta = (opts2 == null ? void 0 : opts2.dontUpdateMeta) ?? false;
      const dontRunListeners = (opts2 == null ? void 0 : opts2.dontRunListeners) ?? false;
      const dontValidate = (opts2 == null ? void 0 : opts2.dontValidate) ?? false;
      batch(() => {
        if (!dontUpdateMeta) {
          this.setFieldMeta(field, (prev) => ({
            ...prev,
            isTouched: true,
            isDirty: true,
            errorMap: {
              // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
              ...prev == null ? void 0 : prev.errorMap,
              onMount: void 0
            }
          }));
        }
        this.baseStore.setState((prev) => {
          return {
            ...prev,
            values: setBy(prev.values, field, updater)
          };
        });
      });
      if (!dontRunListeners) {
        (_a3 = this.getFieldInfo(field).instance) == null ? void 0 : _a3.triggerOnChangeListener();
      }
      if (!dontValidate) {
        this.validateField(field, "change");
      }
    };
    this.deleteField = (field) => {
      const subFieldsToDelete = Object.keys(this.fieldInfo).filter((f) => {
        const fieldStr = field.toString();
        return f !== fieldStr && f.startsWith(fieldStr);
      });
      const fieldsToDelete = [...subFieldsToDelete, field];
      this.baseStore.setState((prev) => {
        const newState = { ...prev };
        fieldsToDelete.forEach((f) => {
          newState.values = deleteBy(newState.values, f);
          delete this.fieldInfo[f];
          delete newState.fieldMetaBase[f];
        });
        return newState;
      });
    };
    this.pushFieldValue = (field, value, options) => {
      this.setFieldValue(
        field,
        (prev) => [...Array.isArray(prev) ? prev : [], value],
        options
      );
      metaHelper(this).bumpArrayVersion(field);
    };
    this.insertFieldValue = async (field, index, value, options) => {
      this.setFieldValue(
        field,
        (prev) => {
          return [
            ...prev.slice(0, index),
            value,
            ...prev.slice(index)
          ];
        },
        mergeOpts(options, { dontValidate: true })
      );
      const dontValidate = (options == null ? void 0 : options.dontValidate) ?? false;
      if (!dontValidate) {
        await this.validateField(field, "change");
      }
      metaHelper(this).handleArrayInsert(field, index);
      if (!dontValidate) {
        await this.validateArrayFieldsStartingFrom(field, index, "change");
      }
    };
    this.replaceFieldValue = async (field, index, value, options) => {
      this.setFieldValue(
        field,
        (prev) => {
          return prev.map(
            (d, i) => i === index ? value : d
          );
        },
        mergeOpts(options, { dontValidate: true })
      );
      metaHelper(this).bumpArrayVersion(field);
      const dontValidate = (options == null ? void 0 : options.dontValidate) ?? false;
      if (!dontValidate) {
        await this.validateField(field, "change");
        await this.validateArrayFieldsStartingFrom(field, index, "change");
      }
    };
    this.removeFieldValue = async (field, index, options) => {
      const fieldValue = this.getFieldValue(field);
      const lastIndex = Array.isArray(fieldValue) ? Math.max(fieldValue.length - 1, 0) : null;
      this.setFieldValue(
        field,
        (prev) => {
          return prev.filter(
            (_d, i) => i !== index
          );
        },
        mergeOpts(options, { dontValidate: true })
      );
      metaHelper(this).handleArrayRemove(field, index);
      if (lastIndex !== null) {
        const start = `${field}[${lastIndex}]`;
        this.deleteField(start);
      }
      const dontValidate = (options == null ? void 0 : options.dontValidate) ?? false;
      if (!dontValidate) {
        await this.validateField(field, "change");
        await this.validateArrayFieldsStartingFrom(field, index, "change");
      }
    };
    this.swapFieldValues = (field, index1, index2, options) => {
      this.setFieldValue(
        field,
        (prev) => {
          const prev1 = prev[index1];
          const prev2 = prev[index2];
          return setBy(setBy(prev, `${index1}`, prev2), `${index2}`, prev1);
        },
        mergeOpts(options, { dontValidate: true })
      );
      metaHelper(this).handleArraySwap(field, index1, index2);
      const dontValidate = (options == null ? void 0 : options.dontValidate) ?? false;
      if (!dontValidate) {
        this.validateField(field, "change");
        this.validateField(`${field}[${index1}]`, "change");
        this.validateField(`${field}[${index2}]`, "change");
      }
    };
    this.moveFieldValues = (field, index1, index2, options) => {
      this.setFieldValue(
        field,
        (prev) => {
          const next = [...prev];
          next.splice(index2, 0, next.splice(index1, 1)[0]);
          return next;
        },
        mergeOpts(options, { dontValidate: true })
      );
      metaHelper(this).handleArrayMove(field, index1, index2);
      const dontValidate = (options == null ? void 0 : options.dontValidate) ?? false;
      if (!dontValidate) {
        this.validateField(field, "change");
        this.validateField(`${field}[${index1}]`, "change");
        this.validateField(`${field}[${index2}]`, "change");
      }
    };
    this.clearFieldValues = (field, options) => {
      const fieldValue = this.getFieldValue(field);
      const lastIndex = Array.isArray(fieldValue) ? Math.max(fieldValue.length - 1, 0) : null;
      this.setFieldValue(
        field,
        [],
        mergeOpts(options, { dontValidate: true })
      );
      metaHelper(this).bumpArrayVersion(field);
      if (lastIndex !== null) {
        for (let i = 0; i <= lastIndex; i++) {
          const fieldKey = `${field}[${i}]`;
          this.deleteField(fieldKey);
        }
      }
      const dontValidate = (options == null ? void 0 : options.dontValidate) ?? false;
      if (!dontValidate) {
        this.validateField(field, "change");
      }
    };
    this.resetField = (field) => {
      this.baseStore.setState((prev) => {
        var _a3;
        const fieldDefault = (_a3 = this.getFieldInfo(field).instance) == null ? void 0 : _a3.options.defaultValue;
        const formDefault = getBy(this.options.defaultValues, field);
        const targetValue = fieldDefault ?? formDefault;
        return {
          ...prev,
          fieldMetaBase: {
            ...prev.fieldMetaBase,
            [field]: defaultFieldMeta
          },
          values: targetValue !== void 0 ? setBy(prev.values, field, targetValue) : prev.values
        };
      });
    };
    this.setErrorMap = (errorMap) => {
      batch(() => {
        Object.entries(errorMap).forEach(([key, value]) => {
          const errorMapKey = key;
          if (isGlobalFormValidationError(value)) {
            const { formError, fieldErrors } = normalizeError(value);
            for (const fieldName of Object.keys(
              this.fieldInfo
            )) {
              const fieldMeta = this.getFieldMeta(fieldName);
              if (!fieldMeta) continue;
              this.setFieldMeta(fieldName, (prev) => ({
                ...prev,
                errorMap: {
                  ...prev.errorMap,
                  [errorMapKey]: fieldErrors == null ? void 0 : fieldErrors[fieldName]
                },
                errorSourceMap: {
                  ...prev.errorSourceMap,
                  [errorMapKey]: "form"
                }
              }));
            }
            this.baseStore.setState((prev) => ({
              ...prev,
              errorMap: {
                ...prev.errorMap,
                [errorMapKey]: formError
              }
            }));
          } else {
            this.baseStore.setState((prev) => ({
              ...prev,
              errorMap: {
                ...prev.errorMap,
                [errorMapKey]: value
              }
            }));
          }
        });
      });
    };
    this.getAllErrors = () => {
      return {
        form: {
          errors: this.state.errors,
          errorMap: this.state.errorMap
        },
        fields: Object.entries(this.state.fieldMeta).reduce(
          (acc, [fieldName, fieldMeta]) => {
            if (Object.keys(fieldMeta).length && fieldMeta.errors.length) {
              acc[fieldName] = {
                errors: fieldMeta.errors,
                errorMap: fieldMeta.errorMap
              };
            }
            return acc;
          },
          {}
        )
      };
    };
    this.parseValuesWithSchema = (schema) => {
      return standardSchemaValidators.validate(
        { value: this.state.values, validationSource: "form" },
        schema
      );
    };
    this.parseValuesWithSchemaAsync = (schema) => {
      return standardSchemaValidators.validateAsync(
        { value: this.state.values, validationSource: "form" },
        schema
      );
    };
    this.timeoutIds = {
      validations: {},
      listeners: {},
      formListeners: {}
    };
    this._formId = (opts == null ? void 0 : opts.formId) ?? uuid();
    this._devtoolsSubmissionOverride = false;
    let baseStoreVal = getDefaultFormState({
      ...opts == null ? void 0 : opts.defaultState,
      values: (opts == null ? void 0 : opts.defaultValues) ?? ((_a2 = opts == null ? void 0 : opts.defaultState) == null ? void 0 : _a2.values)
    });
    if (opts == null ? void 0 : opts.transform) {
      baseStoreVal = opts.transform({ state: baseStoreVal }).state;
      for (const errKey of Object.keys(baseStoreVal.errorMap)) {
        const errKeyMap = baseStoreVal.errorMap[errKey];
        if (errKeyMap === void 0 || !isGlobalFormValidationError(errKeyMap)) {
          continue;
        }
        for (const fieldName of Object.keys(errKeyMap.fields)) {
          const fieldErr = errKeyMap.fields[fieldName];
          if (fieldErr === void 0) {
            continue;
          }
          const existingFieldMeta = baseStoreVal.fieldMetaBase[fieldName];
          baseStoreVal.fieldMetaBase[fieldName] = {
            isTouched: false,
            isValidating: false,
            isBlurred: false,
            isDirty: false,
            _arrayVersion: 0,
            _pendingValidationsCount: 0,
            ...existingFieldMeta ?? {},
            errorSourceMap: {
              ...(existingFieldMeta == null ? void 0 : existingFieldMeta["errorSourceMap"]) ?? {},
              onChange: "form"
            },
            errorMap: {
              ...(existingFieldMeta == null ? void 0 : existingFieldMeta["errorMap"]) ?? {},
              [errKey]: fieldErr
            }
          };
        }
      }
    }
    this.baseStore = createStore(baseStoreVal);
    let prevBaseStore = void 0;
    this.fieldMetaDerived = createStore(
      (prevVal) => {
        var _a3, _b, _c;
        const currBaseStore = this.baseStore.get();
        let originalMetaCount = 0;
        const fieldMeta = {};
        for (const fieldName of Object.keys(
          currBaseStore.fieldMetaBase
        )) {
          const currBaseMeta = currBaseStore.fieldMetaBase[fieldName];
          const prevBaseMeta = prevBaseStore == null ? void 0 : prevBaseStore.fieldMetaBase[fieldName];
          const prevFieldInfo = prevVal == null ? void 0 : prevVal[fieldName];
          const curFieldVal = getBy(currBaseStore.values, fieldName);
          let fieldErrors = prevFieldInfo == null ? void 0 : prevFieldInfo.errors;
          if (!prevBaseMeta || currBaseMeta.errorMap !== prevBaseMeta.errorMap) {
            fieldErrors = Object.values(currBaseMeta.errorMap ?? {}).filter(
              (val) => val !== void 0
            );
            const fieldInstance = (_a3 = this.getFieldInfo(fieldName)) == null ? void 0 : _a3.instance;
            if (!fieldInstance || !fieldInstance.options.disableErrorFlat) {
              fieldErrors = fieldErrors.flat(1);
            }
          }
          const isFieldValid = !isNonEmptyArray(fieldErrors);
          const isFieldPristine = !currBaseMeta.isDirty;
          const isDefaultValue = evaluate(
            curFieldVal,
            // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
            ((_c = (_b = this.getFieldInfo(fieldName)) == null ? void 0 : _b.instance) == null ? void 0 : _c.options.defaultValue) ?? getBy(this.options.defaultValues, fieldName)
          );
          if (prevFieldInfo && prevFieldInfo.isPristine === isFieldPristine && prevFieldInfo.isValid === isFieldValid && prevFieldInfo.isDefaultValue === isDefaultValue && prevFieldInfo.errors === fieldErrors && currBaseMeta === prevBaseMeta) {
            fieldMeta[fieldName] = prevFieldInfo;
            originalMetaCount++;
            continue;
          }
          fieldMeta[fieldName] = {
            ...currBaseMeta,
            errors: fieldErrors ?? [],
            isPristine: isFieldPristine,
            isValid: isFieldValid,
            isDefaultValue
          };
        }
        if (!Object.keys(currBaseStore.fieldMetaBase).length) return fieldMeta;
        if (prevVal && originalMetaCount === Object.keys(currBaseStore.fieldMetaBase).length) {
          return prevVal;
        }
        prevBaseStore = this.baseStore.get();
        return fieldMeta;
      }
    );
    this.formGroupMetaDerived = createStore(
      (prevVal) => {
        const currBaseStore = this.baseStore.get();
        const currFieldMeta = this.fieldMetaDerived.get();
        const result = {};
        for (const group of this.formGroupApis) {
          const groupName = group.name;
          const lifecycle = currBaseStore.formGroupStateBase[groupName] ?? {
            isSubmitted: false,
            isSubmitting: false,
            isValidating: false,
            submissionAttempts: 0,
            isSubmitSuccessful: false
          };
          const ownFieldMeta = currFieldMeta[groupName];
          let isFieldsValidating = false;
          let isFieldsValid = true;
          let aggIsTouched = false;
          let aggIsBlurred = false;
          let aggIsDefaultValue = true;
          let aggIsDirty = false;
          for (const fieldName in currFieldMeta) {
            if (fieldName === groupName) continue;
            if (!isFieldInGroup(groupName, fieldName)) continue;
            const m = currFieldMeta[fieldName];
            if (!m) continue;
            if (m.isValidating) isFieldsValidating = true;
            if (!m.isValid) isFieldsValid = false;
            if (m.isTouched) aggIsTouched = true;
            if (m.isBlurred) aggIsBlurred = true;
            if (!m.isDefaultValue) aggIsDefaultValue = false;
            if (m.isDirty) aggIsDirty = true;
          }
          const isPristine = !aggIsDirty;
          const isValidating = !!isFieldsValidating || lifecycle.isValidating;
          const errorMap = (ownFieldMeta == null ? void 0 : ownFieldMeta.errorMap) ?? {};
          const errorSourceMap = (ownFieldMeta == null ? void 0 : ownFieldMeta.errorSourceMap) ?? {};
          const hasOnMountError = Boolean(
            errorMap.onMount || Object.entries(currFieldMeta).some(
              ([fieldName, field]) => field && fieldName !== groupName && isFieldInGroup(groupName, fieldName) && field.errorMap.onMount
            )
          );
          const prevGroupMeta = prevVal == null ? void 0 : prevVal[groupName];
          let errors = (prevGroupMeta == null ? void 0 : prevGroupMeta.errors) ?? [];
          if (!prevGroupMeta || prevGroupMeta.__srcErrorMap !== errorMap) {
            errors = Object.values(errorMap).reduce((acc, curr) => {
              if (curr === void 0) return acc;
              if (curr && typeof curr === "object" && "fields" in curr) {
                const groupErr = curr.group;
                if (groupErr !== void 0) acc.push(groupErr);
                return acc;
              }
              acc.push(curr);
              return acc;
            }, []);
          }
          const isGroupValid = errors.length === 0;
          const isValid = isFieldsValid && isGroupValid;
          const submitInvalid = group.options.canSubmitWhenInvalid ?? false;
          const canSubmit = lifecycle.submissionAttempts === 0 && !aggIsTouched && !hasOnMountError || !isValidating && !lifecycle.isSubmitting && isValid || submitInvalid;
          if (prevGroupMeta && prevGroupMeta.errorMap === errorMap && prevGroupMeta.errorSourceMap === errorSourceMap && prevGroupMeta.errors === errors && prevGroupMeta.isFieldsValidating === isFieldsValidating && prevGroupMeta.isFieldsValid === isFieldsValid && prevGroupMeta.isGroupValid === isGroupValid && prevGroupMeta.isValid === isValid && prevGroupMeta.canSubmit === canSubmit && prevGroupMeta.isTouched === aggIsTouched && prevGroupMeta.isBlurred === aggIsBlurred && prevGroupMeta.isPristine === isPristine && prevGroupMeta.isDefaultValue === aggIsDefaultValue && prevGroupMeta.isDirty === aggIsDirty && prevGroupMeta.isValidating === isValidating && prevGroupMeta.isSubmitting === lifecycle.isSubmitting && prevGroupMeta.isSubmitted === lifecycle.isSubmitted && prevGroupMeta.submissionAttempts === lifecycle.submissionAttempts && prevGroupMeta.isSubmitSuccessful === lifecycle.isSubmitSuccessful) {
            result[groupName] = prevGroupMeta;
            continue;
          }
          const meta = {
            // Submission lifecycle (spread first; `isValidating` below
            // intentionally overrides `lifecycle.isValidating` with the
            // OR of group-level + descendant-field validating).
            ...lifecycle,
            // Field-meta-base fields (so `setMeta` updates can roundtrip
            // through `state.meta`).
            errorMap,
            errorSourceMap,
            _arrayVersion: (ownFieldMeta == null ? void 0 : ownFieldMeta._arrayVersion) ?? 0,
            // Aggregated descendant booleans (override field-level meaning
            // for groups — a group's "field" itself never receives input).
            isTouched: aggIsTouched,
            isBlurred: aggIsBlurred,
            isDirty: aggIsDirty,
            isPristine,
            isDefaultValue: aggIsDefaultValue,
            // Aggregated validity
            isValid,
            errors,
            isValidating,
            // Group-only flags
            isFieldsValidating,
            isFieldsValid,
            isGroupValid,
            canSubmit
          };
          Object.defineProperty(meta, "__srcErrorMap", {
            value: errorMap,
            enumerable: false,
            configurable: true
          });
          result[groupName] = meta;
        }
        return result;
      }
    );
    let prevBaseStoreForStore = void 0;
    this.store = createStore((prevVal) => {
      var _a3, _b;
      const currBaseStore = this.baseStore.get();
      const currFieldMeta = this.fieldMetaDerived.get();
      const fieldMetaValues = Object.values(currFieldMeta).filter(
        Boolean
      );
      const isFieldsValidating = fieldMetaValues.some(
        (field) => field.isValidating
      );
      const isFieldsValid = fieldMetaValues.every((field) => field.isValid);
      const isTouched = fieldMetaValues.some((field) => field.isTouched);
      const isBlurred = fieldMetaValues.some((field) => field.isBlurred);
      const isDefaultValue = fieldMetaValues.every(
        (field) => field.isDefaultValue
      );
      const shouldInvalidateOnMount = (
        // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        isTouched && ((_a3 = currBaseStore.errorMap) == null ? void 0 : _a3.onMount)
      );
      const isDirty = fieldMetaValues.some((field) => field.isDirty);
      const isPristine = !isDirty;
      const hasOnMountError = Boolean(
        // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        ((_b = currBaseStore.errorMap) == null ? void 0 : _b.onMount) || // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        fieldMetaValues.some((f) => {
          var _a4;
          return (_a4 = f == null ? void 0 : f.errorMap) == null ? void 0 : _a4.onMount;
        })
      );
      const isValidating = !!isFieldsValidating;
      let errors = (prevVal == null ? void 0 : prevVal.errors) ?? [];
      if (!prevBaseStoreForStore || currBaseStore.errorMap !== prevBaseStoreForStore.errorMap) {
        errors = Object.values(currBaseStore.errorMap).reduce((prev, curr) => {
          if (curr === void 0) return prev;
          if (curr && isGlobalFormValidationError(curr)) {
            prev.push(curr.form);
            return prev;
          }
          prev.push(curr);
          return prev;
        }, []);
      }
      const isFormValid = errors.length === 0;
      const isValid = isFieldsValid && isFormValid;
      const submitInvalid = this.options.canSubmitWhenInvalid ?? false;
      const canSubmit = currBaseStore.submissionAttempts === 0 && !isTouched && !hasOnMountError || !isValidating && !currBaseStore.isSubmitting && isValid || submitInvalid;
      let errorMap = currBaseStore.errorMap;
      if (shouldInvalidateOnMount) {
        errors = errors.filter((err) => err !== currBaseStore.errorMap.onMount);
        errorMap = Object.assign(errorMap, { onMount: void 0 });
      }
      if (prevVal && prevBaseStoreForStore && prevVal.errorMap === errorMap && prevVal.fieldMeta === this.fieldMetaDerived.state && prevVal.errors === errors && prevVal.isFieldsValidating === isFieldsValidating && prevVal.isFieldsValid === isFieldsValid && prevVal.isFormValid === isFormValid && prevVal.isValid === isValid && prevVal.canSubmit === canSubmit && prevVal.isTouched === isTouched && prevVal.isBlurred === isBlurred && prevVal.isPristine === isPristine && prevVal.isDefaultValue === isDefaultValue && prevVal.isDirty === isDirty && evaluate(prevBaseStoreForStore, currBaseStore)) {
        return prevVal;
      }
      const state = {
        ...currBaseStore,
        errorMap,
        fieldMeta: this.fieldMetaDerived.state,
        errors,
        isFieldsValidating,
        isFieldsValid,
        isFormValid,
        isValid,
        canSubmit,
        isTouched,
        isBlurred,
        isPristine,
        isDefaultValue,
        isDirty
      };
      prevBaseStoreForStore = this.baseStore.get();
      return state;
    });
    this.handleSubmit = this.handleSubmit.bind(this);
    this.update(opts || {});
  }
  get state() {
    return this.store.state;
  }
  get formId() {
    return this._formId;
  }
  /**
   * @private
   */
  runValidator(props) {
    if (isStandardSchemaValidator(props.validate)) {
      return standardSchemaValidators[props.type](
        props.value,
        props.validate
      );
    }
    return props.validate(props.value);
  }
  handleSubmit(submitMeta) {
    return this._handleSubmit(submitMeta);
  }
};
function normalizeError(rawError) {
  if (rawError) {
    if (isGlobalFormValidationError(rawError)) {
      const formError = normalizeError(rawError.form).formError;
      const fieldErrors = rawError.fields;
      return { formError, fieldErrors };
    }
    return { formError: rawError };
  }
  return { formError: void 0 };
}
function getErrorMapKey(cause) {
  switch (cause) {
    case "submit":
      return "onSubmit";
    case "blur":
      return "onBlur";
    case "mount":
      return "onMount";
    case "server":
      return "onServer";
    case "dynamic":
      return "onDynamic";
    case "change":
    default:
      return "onChange";
  }
}

// node_modules/@tanstack/form-core/dist/esm/FieldApi.js
var FieldApi = class _FieldApi {
  /**
   * Initializes a new `FieldApi` instance.
   */
  constructor(opts) {
    this.options = {};
    this.mount = () => {
      var _a2, _b;
      if (this.options.defaultValue !== void 0 && !this.getMeta().isTouched) {
        this.form.setFieldValue(this.name, this.options.defaultValue, {
          dontUpdateMeta: true
        });
      }
      const info = this.getInfo();
      info.instance = this;
      this.update(this.options);
      const { onMount } = this.options.validators || {};
      if (onMount) {
        const error = this.runValidator({
          validate: onMount,
          value: {
            value: this.state.value,
            fieldApi: this,
            validationSource: "field"
          },
          type: "validate"
        });
        if (error) {
          this.setMeta(
            (prev) => ({
              ...prev,
              // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
              errorMap: { ...prev == null ? void 0 : prev.errorMap, onMount: error },
              errorSourceMap: {
                // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                ...prev == null ? void 0 : prev.errorSourceMap,
                onMount: "field"
              }
            })
          );
        }
      }
      (_b = (_a2 = this.options.listeners) == null ? void 0 : _a2.onMount) == null ? void 0 : _b.call(_a2, {
        value: this.state.value,
        fieldApi: this
      });
      return () => {
        var _a3, _b2, _c, _d;
        for (const [key, timeout] of Object.entries(
          this.timeoutIds.validations
        )) {
          if (timeout) {
            clearTimeout(timeout);
            this.timeoutIds.validations[key] = null;
          }
        }
        for (const [key, timeout] of Object.entries(this.timeoutIds.listeners)) {
          if (timeout) {
            clearTimeout(timeout);
            this.timeoutIds.listeners[key] = null;
          }
        }
        for (const [key, timeout] of Object.entries(
          this.timeoutIds.formListeners
        )) {
          if (timeout) {
            clearTimeout(timeout);
            this.timeoutIds.formListeners[key] = null;
          }
        }
        const fieldInfo = this.form.fieldInfo[this.name];
        if (!fieldInfo) return;
        if (fieldInfo.instance !== this) return;
        for (const [key, validationMeta] of Object.entries(
          fieldInfo.validationMetaMap
        )) {
          validationMeta == null ? void 0 : validationMeta.lastAbortController.abort();
          fieldInfo.validationMetaMap[key] = void 0;
        }
        this.form.baseStore.setState((prev) => {
          var _a4, _b3, _c2;
          return {
            // Preserve interaction flags so field-level defaultValue does not
            // reseed user-entered values on remount.
            ...prev,
            fieldMetaBase: {
              ...prev.fieldMetaBase,
              [this.name]: {
                ...defaultFieldMeta,
                isTouched: ((_a4 = prev.fieldMetaBase[this.name]) == null ? void 0 : _a4.isTouched) ?? defaultFieldMeta.isTouched,
                isBlurred: ((_b3 = prev.fieldMetaBase[this.name]) == null ? void 0 : _b3.isBlurred) ?? defaultFieldMeta.isBlurred,
                isDirty: ((_c2 = prev.fieldMetaBase[this.name]) == null ? void 0 : _c2.isDirty) ?? defaultFieldMeta.isDirty
              }
            }
          };
        });
        fieldInfo.instance = null;
        (_b2 = (_a3 = this.options.listeners) == null ? void 0 : _a3.onUnmount) == null ? void 0 : _b2.call(_a3, {
          value: this.state.value,
          fieldApi: this
        });
        (_d = (_c = this.form.options.listeners) == null ? void 0 : _c.onFieldUnmount) == null ? void 0 : _d.call(_c, {
          formApi: this.form,
          fieldApi: this
        });
      };
    };
    this.update = (opts2) => {
      this.options = opts2;
      this.name = opts2.name;
      if (!this.state.meta.isTouched && this.options.defaultValue !== void 0) {
        const formField = this.form.getFieldValue(this.name);
        if (!evaluate(formField, opts2.defaultValue)) {
          this.form.setFieldValue(this.name, opts2.defaultValue, {
            dontUpdateMeta: true,
            dontValidate: true,
            dontRunListeners: true
          });
        }
      }
      if (!this.form.getFieldMeta(this.name)) {
        this.form.setFieldMeta(this.name, this.state.meta);
      }
    };
    this.getValue = () => {
      return this.form.getFieldValue(this.name);
    };
    this.setValue = (updater, options) => {
      this.form.setFieldValue(
        this.name,
        updater,
        mergeOpts(options, { dontRunListeners: true, dontValidate: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
      if (!(options == null ? void 0 : options.dontValidate)) {
        this.validate("change");
      }
    };
    this.getMeta = () => this.store.state.meta;
    this.setMeta = (updater) => this.form.setFieldMeta(this.name, updater);
    this.getInfo = () => this.form.getFieldInfo(this.name);
    this.pushValue = (value, options) => {
      this.form.pushFieldValue(
        this.name,
        value,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.insertValue = (index, value, options) => {
      this.form.insertFieldValue(
        this.name,
        index,
        value,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.replaceValue = (index, value, options) => {
      this.form.replaceFieldValue(
        this.name,
        index,
        value,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.removeValue = (index, options) => {
      this.form.removeFieldValue(
        this.name,
        index,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.swapValues = (aIndex, bIndex, options) => {
      this.form.swapFieldValues(
        this.name,
        aIndex,
        bIndex,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.moveValue = (aIndex, bIndex, options) => {
      this.form.moveFieldValues(
        this.name,
        aIndex,
        bIndex,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.clearValues = (options) => {
      this.form.clearFieldValues(
        this.name,
        mergeOpts(options, { dontRunListeners: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
    };
    this.getLinkedFields = (cause) => {
      const fields = Object.values(this.form.fieldInfo);
      const linkedFields = [];
      for (const field of fields) {
        if (!field.instance) continue;
        if (!(field.instance instanceof _FieldApi)) {
          continue;
        }
        const { onChangeListenTo, onBlurListenTo } = field.instance.options.validators || {};
        if (cause === "change" && (onChangeListenTo == null ? void 0 : onChangeListenTo.includes(this.name))) {
          linkedFields.push(field.instance);
        }
        if (cause === "blur" && (onBlurListenTo == null ? void 0 : onBlurListenTo.includes(this.name))) {
          linkedFields.push(field.instance);
        }
      }
      return linkedFields;
    };
    this.validateSync = (cause, errorFromForm) => {
      var _a2;
      const validates = getSyncValidatorArray(cause, {
        ...this.options,
        form: this.form,
        fieldName: this.name,
        validationLogic: this.form.options.validationLogic || defaultValidationLogic
      });
      const linkedFields = this.getLinkedFields(cause);
      const linkedFieldValidates = linkedFields.reduce(
        (acc, field) => {
          const fieldValidates = getSyncValidatorArray(cause, {
            ...field.options,
            form: field.form,
            fieldName: field.name,
            validationLogic: field.form.options.validationLogic || defaultValidationLogic
          });
          fieldValidates.forEach((validate) => {
            validate.field = field;
          });
          return acc.concat(fieldValidates);
        },
        []
      );
      let hasErrored = false;
      batch(() => {
        const validateFieldFn = (field, validateObj) => {
          var _a3;
          const errorMapKey = getErrorMapKey2(validateObj.cause);
          const fieldLevelError = validateObj.validate ? normalizeError2(
            field.runValidator({
              validate: validateObj.validate,
              value: {
                value: field.store.state.value,
                validationSource: "field",
                fieldApi: field
              },
              type: "validate"
            })
          ) : void 0;
          const formLevelError = errorFromForm[errorMapKey];
          const { newErrorValue, newSource } = determineFieldLevelErrorSourceAndValue({
            formLevelError,
            fieldLevelError
          });
          if (((_a3 = field.state.meta.errorMap) == null ? void 0 : _a3[errorMapKey]) !== newErrorValue) {
            field.setMeta((prev) => ({
              ...prev,
              errorMap: {
                ...prev.errorMap,
                [errorMapKey]: newErrorValue
              },
              errorSourceMap: {
                ...prev.errorSourceMap,
                [errorMapKey]: newSource
              }
            }));
          }
          if (newErrorValue) {
            hasErrored = true;
          }
        };
        for (const validateObj of validates) {
          validateFieldFn(this, validateObj);
        }
        for (const fieldValitateObj of linkedFieldValidates) {
          if (!fieldValitateObj.validate) continue;
          validateFieldFn(fieldValitateObj.field, fieldValitateObj);
        }
      });
      const submitErrKey = getErrorMapKey2("submit");
      if (
        // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        ((_a2 = this.state.meta.errorMap) == null ? void 0 : _a2[submitErrKey]) && cause !== "submit" && !hasErrored
      ) {
        this.setMeta((prev) => ({
          ...prev,
          errorMap: {
            ...prev.errorMap,
            [submitErrKey]: void 0
          },
          errorSourceMap: {
            ...prev.errorSourceMap,
            [submitErrKey]: void 0
          }
        }));
      }
      return { hasErrored };
    };
    this.validateAsync = async (cause, formValidationResultPromise) => {
      const validates = getAsyncValidatorArray(cause, {
        ...this.options,
        form: this.form,
        fieldName: this.name,
        validationLogic: this.form.options.validationLogic || defaultValidationLogic
      });
      const asyncFormValidationResults = await formValidationResultPromise;
      const linkedFields = this.getLinkedFields(cause);
      const linkedFieldValidates = linkedFields.reduce(
        (acc, field) => {
          const fieldValidates = getAsyncValidatorArray(cause, {
            ...field.options,
            form: field.form,
            fieldName: field.name,
            validationLogic: field.form.options.validationLogic || defaultValidationLogic
          });
          fieldValidates.forEach((validate) => {
            validate.field = field;
          });
          return acc.concat(fieldValidates);
        },
        []
      );
      const validatesPromises = [];
      const linkedPromises = [];
      const hasAsyncValidators = validates.some((v) => v.validate);
      const linkedFieldsWithAsyncValidators = Array.from(
        new Set(
          linkedFieldValidates.filter((v) => v.validate).map((v) => v.field)
        )
      );
      batch(() => {
        if (hasAsyncValidators) {
          this.startValidation();
        }
        for (const linkedField of linkedFieldsWithAsyncValidators) {
          linkedField.startValidation();
        }
      });
      const validateFieldAsyncFn = (field, validateObj, promises) => {
        const errorMapKey = getErrorMapKey2(validateObj.cause);
        const fieldInfo = field.getInfo();
        const fieldValidatorMeta = fieldInfo.validationMetaMap[errorMapKey];
        fieldValidatorMeta == null ? void 0 : fieldValidatorMeta.lastAbortController.abort();
        const controller = new AbortController();
        fieldInfo.validationMetaMap[errorMapKey] = {
          lastAbortController: controller
        };
        promises.push(
          new Promise(async (resolve) => {
            var _a2;
            let rawError;
            try {
              rawError = await new Promise((rawResolve, rawReject) => {
                if (field.timeoutIds.validations[validateObj.cause]) {
                  clearTimeout(field.timeoutIds.validations[validateObj.cause]);
                  field.endValidation();
                }
                field.timeoutIds.validations[validateObj.cause] = setTimeout(
                  async () => {
                    if (controller.signal.aborted) return rawResolve(void 0);
                    try {
                      rawResolve(
                        await this.runValidator({
                          validate: validateObj.validate,
                          value: {
                            value: field.store.state.value,
                            fieldApi: field,
                            signal: controller.signal,
                            validationSource: "field"
                          },
                          type: "validateAsync"
                        })
                      );
                    } catch (e) {
                      rawReject(e);
                    }
                  },
                  validateObj.debounceMs
                );
              });
            } catch (e) {
              rawError = e;
            }
            if (controller.signal.aborted) return resolve(void 0);
            const fieldLevelError = normalizeError2(rawError);
            const formLevelError = (_a2 = asyncFormValidationResults[field.name]) == null ? void 0 : _a2[errorMapKey];
            const { newErrorValue, newSource } = determineFieldLevelErrorSourceAndValue({
              formLevelError,
              fieldLevelError
            });
            if (field.getInfo().instance !== field) {
              return resolve(void 0);
            }
            field.setMeta((prev) => {
              return {
                ...prev,
                errorMap: {
                  // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                  ...prev == null ? void 0 : prev.errorMap,
                  [errorMapKey]: newErrorValue
                },
                errorSourceMap: {
                  ...prev.errorSourceMap,
                  [errorMapKey]: newSource
                }
              };
            });
            resolve(newErrorValue);
          })
        );
      };
      for (const validateObj of validates) {
        if (!validateObj.validate) continue;
        validateFieldAsyncFn(this, validateObj, validatesPromises);
      }
      for (const fieldValitateObj of linkedFieldValidates) {
        if (!fieldValitateObj.validate) continue;
        validateFieldAsyncFn(
          fieldValitateObj.field,
          fieldValitateObj,
          linkedPromises
        );
      }
      let results = [];
      if (validatesPromises.length || linkedPromises.length) {
        results = await Promise.all(validatesPromises);
        await Promise.all(linkedPromises);
      }
      batch(() => {
        if (hasAsyncValidators) {
          this.endValidation();
        }
        for (const linkedField of linkedFieldsWithAsyncValidators) {
          linkedField.endValidation();
        }
      });
      return results.filter(Boolean);
    };
    this.validate = (cause, opts2) => {
      var _a2, _b;
      if (!this.state.meta.isTouched) return [];
      const encompassingGroups = (opts2 == null ? void 0 : opts2.skipGroupValidation) ? [] : Array.from(this.form.formGroupApis).filter(
        (group) => this.name.startsWith(group.name)
      );
      const formSyncResult = (opts2 == null ? void 0 : opts2.skipFormValidation) ? { fieldsErrorMap: {} } : this.form.validateSync(cause);
      let fieldsErrorMap = formSyncResult.fieldsErrorMap[this.name] ?? {};
      if (!(opts2 == null ? void 0 : opts2.skipFormValidation)) {
        for (const group of encompassingGroups) {
          if (group.state.meta.submissionAttempts === 0) continue;
          const { fieldsErrorMap: groupFormErrors } = this.form.validateSync(
            cause,
            {
              group,
              dontUpdateFormErrorMap: true,
              filterFieldNames: (fieldName) => isFieldInGroup(group.name, fieldName)
            }
          );
          fieldsErrorMap = {
            ...fieldsErrorMap,
            ...groupFormErrors[this.name] ?? {}
          };
        }
      }
      const { hasErrored } = this.validateSync(cause, fieldsErrorMap);
      const groupHasErroredWeakMap = /* @__PURE__ */ new WeakMap();
      for (const group of encompassingGroups) {
        const { hasErrored: groupHasErrored } = group.validateSync(
          cause,
          {},
          { skipRelatedFieldValidation: true }
        );
        groupHasErroredWeakMap.set(group, groupHasErrored);
      }
      if (hasErrored && !this.options.asyncAlways) {
        (_a2 = this.getInfo().validationMetaMap[getErrorMapKey2(cause)]) == null ? void 0 : _a2.lastAbortController.abort();
        const groupErrors = [];
        for (const group of encompassingGroups) {
          (_b = group.getInfo().validationMetaMap[getErrorMapKey2(cause)]) == null ? void 0 : _b.lastAbortController.abort();
          groupErrors.push(group.state.meta.errors);
        }
        return [...this.state.meta.errors, ...groupErrors.flat()];
      }
      const formValidationResultPromise = (opts2 == null ? void 0 : opts2.skipFormValidation) ? Promise.resolve({}) : this.form.validateAsync(cause);
      const fieldAsyncResults = this.validateAsync(
        cause,
        formValidationResultPromise
      );
      const groupAsyncResults = [];
      for (const group of encompassingGroups) {
        if (groupHasErroredWeakMap.get(group) && !group.options.asyncAlways) {
          continue;
        }
        groupAsyncResults.push(
          group.validateAsync(cause, formValidationResultPromise, {
            skipRelatedFieldValidation: true
          })
        );
      }
      if (groupAsyncResults.length === 0) {
        return fieldAsyncResults;
      }
      return Promise.all([fieldAsyncResults, ...groupAsyncResults]).then(
        (results) => results.flat()
      );
    };
    this.handleChange = (updater) => {
      this.setValue(updater);
    };
    this.handleBlur = () => {
      const prevTouched = this.state.meta.isTouched;
      if (!prevTouched) {
        this.setMeta((prev) => ({ ...prev, isTouched: true }));
      }
      if (!this.state.meta.isBlurred) {
        this.setMeta((prev) => ({ ...prev, isBlurred: true }));
      }
      this.validate("blur");
      this.triggerOnBlurListener();
    };
    this.setErrorMap = (errorMap) => {
      this.setMeta((prev) => ({
        ...prev,
        errorMap: {
          ...prev.errorMap,
          ...errorMap
        }
      }));
    };
    this.parseValueWithSchema = (schema) => {
      return standardSchemaValidators.validate(
        { value: this.state.value, validationSource: "field" },
        schema
      );
    };
    this.parseValueWithSchemaAsync = (schema) => {
      return standardSchemaValidators.validateAsync(
        { value: this.state.value, validationSource: "field" },
        schema
      );
    };
    this.triggerOnBlurListener = () => {
      var _a2, _b, _c, _d, _e, _f;
      const formDebounceMs = (_a2 = this.form.options.listeners) == null ? void 0 : _a2.onBlurDebounceMs;
      if (formDebounceMs && formDebounceMs > 0) {
        if (this.timeoutIds.formListeners.blur) {
          clearTimeout(this.timeoutIds.formListeners.blur);
        }
        this.timeoutIds.formListeners.blur = setTimeout(() => {
          var _a3, _b2;
          (_b2 = (_a3 = this.form.options.listeners) == null ? void 0 : _a3.onBlur) == null ? void 0 : _b2.call(_a3, {
            formApi: this.form,
            fieldApi: this
          });
        }, formDebounceMs);
      } else {
        (_c = (_b = this.form.options.listeners) == null ? void 0 : _b.onBlur) == null ? void 0 : _c.call(_b, {
          formApi: this.form,
          fieldApi: this
        });
      }
      const fieldDebounceMs = (_d = this.options.listeners) == null ? void 0 : _d.onBlurDebounceMs;
      if (fieldDebounceMs && fieldDebounceMs > 0) {
        if (this.timeoutIds.listeners.blur) {
          clearTimeout(this.timeoutIds.listeners.blur);
        }
        this.timeoutIds.listeners.blur = setTimeout(() => {
          var _a3, _b2;
          (_b2 = (_a3 = this.options.listeners) == null ? void 0 : _a3.onBlur) == null ? void 0 : _b2.call(_a3, {
            value: this.state.value,
            fieldApi: this
          });
        }, fieldDebounceMs);
      } else {
        (_f = (_e = this.options.listeners) == null ? void 0 : _e.onBlur) == null ? void 0 : _f.call(_e, {
          value: this.state.value,
          fieldApi: this
        });
      }
    };
    this.triggerOnChangeListener = () => {
      var _a2, _b, _c, _d, _e, _f;
      const formDebounceMs = (_a2 = this.form.options.listeners) == null ? void 0 : _a2.onChangeDebounceMs;
      if (formDebounceMs && formDebounceMs > 0) {
        if (this.timeoutIds.formListeners.change) {
          clearTimeout(this.timeoutIds.formListeners.change);
        }
        this.timeoutIds.formListeners.change = setTimeout(() => {
          var _a3, _b2;
          (_b2 = (_a3 = this.form.options.listeners) == null ? void 0 : _a3.onChange) == null ? void 0 : _b2.call(_a3, {
            formApi: this.form,
            fieldApi: this
          });
        }, formDebounceMs);
      } else {
        (_c = (_b = this.form.options.listeners) == null ? void 0 : _b.onChange) == null ? void 0 : _c.call(_b, {
          formApi: this.form,
          fieldApi: this
        });
      }
      const fieldDebounceMs = (_d = this.options.listeners) == null ? void 0 : _d.onChangeDebounceMs;
      if (fieldDebounceMs && fieldDebounceMs > 0) {
        if (this.timeoutIds.listeners.change) {
          clearTimeout(this.timeoutIds.listeners.change);
        }
        this.timeoutIds.listeners.change = setTimeout(() => {
          var _a3, _b2;
          (_b2 = (_a3 = this.options.listeners) == null ? void 0 : _a3.onChange) == null ? void 0 : _b2.call(_a3, {
            value: this.state.value,
            fieldApi: this
          });
        }, fieldDebounceMs);
      } else {
        (_f = (_e = this.options.listeners) == null ? void 0 : _e.onChange) == null ? void 0 : _f.call(_e, {
          value: this.state.value,
          fieldApi: this
        });
      }
      for (const group of this.form.formGroupApis) {
        if (isFieldInGroup(group.name, this.name)) {
          group.triggerOnChangeListener();
        }
      }
    };
    this.triggerOnSubmitListener = () => {
      var _a2, _b;
      (_b = (_a2 = this.options.listeners) == null ? void 0 : _a2.onSubmit) == null ? void 0 : _b.call(_a2, {
        value: this.state.value,
        fieldApi: this
      });
    };
    this.form = opts.form;
    this.name = opts.name;
    this.options = opts;
    this.timeoutIds = {
      validations: {},
      listeners: {},
      formListeners: {}
    };
    this.store = createStore(
      (prevVal) => {
        this.form.store.get();
        const meta = this.form.getFieldMeta(this.name) ?? {
          ...defaultFieldMeta,
          ...opts.defaultMeta
        };
        let value = this.form.getFieldValue(this.name);
        if (!meta.isTouched && value === void 0 && this.options.defaultValue !== void 0 && !evaluate(value, this.options.defaultValue)) {
          value = this.options.defaultValue;
        }
        if (prevVal && prevVal.value === value && prevVal.meta === meta) {
          return prevVal;
        }
        return {
          value,
          meta
        };
      }
    );
  }
  /**
   * The current field state.
   */
  get state() {
    return this.store.state;
  }
  /**
   * @private
   */
  runValidator(props) {
    if (isStandardSchemaValidator(props.validate)) {
      return standardSchemaValidators[props.type](
        props.value,
        props.validate
      );
    }
    return props.validate(props.value);
  }
  /**
   * `@private`
   * Starts tracking an async validation, incrementing the counter and setting isValidating if needed.
   */
  startValidation() {
    this.setMeta((prev) => {
      const newCount = prev._pendingValidationsCount + 1;
      return {
        ...prev,
        _pendingValidationsCount: newCount,
        isValidating: newCount > 0 && !prev.isValidating ? true : prev.isValidating
      };
    });
  }
  /**
   * `@private`
   * Ends tracking an async validation, decrementing the counter and clearing isValidating if no validations remain.
   */
  endValidation() {
    this.setMeta((prev) => {
      const newCount = Math.max(0, prev._pendingValidationsCount - 1);
      return {
        ...prev,
        _pendingValidationsCount: newCount,
        isValidating: newCount === 0 && prev.isValidating ? false : prev.isValidating
      };
    });
  }
};
function normalizeError2(rawError) {
  if (rawError) {
    return rawError;
  }
  return void 0;
}
function getErrorMapKey2(cause) {
  switch (cause) {
    case "submit":
      return "onSubmit";
    case "blur":
      return "onBlur";
    case "mount":
      return "onMount";
    case "server":
      return "onServer";
    case "dynamic":
      return "onDynamic";
    case "change":
    default:
      return "onChange";
  }
}

// node_modules/@tanstack/form-core/dist/esm/FormGroupApi.js
function getDefaultFormGroupState(defaultState) {
  return {
    isSubmitted: defaultState.isSubmitted ?? false,
    isSubmitting: defaultState.isSubmitting ?? false,
    isValidating: defaultState.isValidating ?? false,
    submissionAttempts: defaultState.submissionAttempts ?? 0,
    isSubmitSuccessful: defaultState.isSubmitSuccessful ?? false
  };
}
function getDefaultFormGroupMeta(defaultMeta) {
  return {
    ...defaultFieldMeta,
    ...defaultMeta,
    errors: [],
    isPristine: true,
    isValid: true,
    isDefaultValue: true,
    isFieldsValidating: false,
    isFieldsValid: true,
    isGroupValid: true,
    canSubmit: true,
    isSubmitting: false,
    isSubmitted: false,
    isValidating: false,
    submissionAttempts: 0,
    isSubmitSuccessful: false
  };
}
var FormGroupApi = class _FormGroupApi {
  constructor(opts) {
    this.options = {};
    this.setFormGroupState = (updater) => {
      this.form.baseStore.setState((prev) => {
        const prevGroupState = prev.formGroupStateBase[this.name] ?? getDefaultFormGroupState({});
        return {
          ...prev,
          formGroupStateBase: {
            ...prev.formGroupStateBase,
            [this.name]: updater(prevGroupState)
          }
        };
      });
    };
    this._lastDistributedFieldNames = {};
    this.update = (opts2) => {
      this.options = opts2;
      this.name = opts2.name;
      if (!this.state.meta.isTouched && this.options.defaultValue !== void 0) {
        const formField = this.form.getFieldValue(this.name);
        if (!evaluate(formField, opts2.defaultValue)) {
          this.form.setFieldValue(this.name, opts2.defaultValue, {
            dontUpdateMeta: true,
            dontValidate: true,
            dontRunListeners: true
          });
        }
      }
      if (!this.form.getFieldMeta(this.name)) {
        this.form.setFieldMeta(this.name, {
          ...defaultFieldMeta,
          ...this.options.defaultMeta
        });
      }
    };
    this.mount = () => {
      var _a2, _b;
      this.update(this.options);
      this.form.formGroupApis.add(this);
      this.fieldInfo.instance = this;
      this.form.baseStore.setState((prev) => ({
        ...prev,
        formGroupStateBase: {
          ...prev.formGroupStateBase,
          [this.name]: prev.formGroupStateBase[this.name] ?? getDefaultFormGroupState({
            ...this.options.defaultState
          })
        }
      }));
      const { onMount } = this.options.validators || {};
      if (onMount) {
        const rawError = this.runValidator({
          validate: onMount,
          value: {
            value: this.state.value,
            groupApi: this,
            validationSource: "form"
          },
          type: "validate"
        });
        let groupOwnRawError = rawError;
        let groupFieldErrors = void 0;
        if (isGlobalGroupValidationError(rawError)) {
          groupOwnRawError = rawError.group;
          groupFieldErrors = rawError.fields;
        }
        const error = normalizeError3(groupOwnRawError);
        if (error) {
          this.setMeta(
            (prev) => ({
              ...prev,
              errorMap: {
                ...prev.errorMap,
                onMount: error
              },
              errorSourceMap: {
                ...prev.errorSourceMap,
                onMount: "field"
              }
            })
          );
        }
        this.distributeFieldErrors("onMount", groupFieldErrors);
      }
      (_b = (_a2 = this.options.listeners) == null ? void 0 : _a2.onMount) == null ? void 0 : _b.call(_a2, {
        value: this.state.value,
        groupApi: this
      });
      return () => {
        var _a3, _b2;
        for (const [key, timeout] of Object.entries(
          this.timeoutIds.validations
        )) {
          if (timeout) {
            clearTimeout(timeout);
            this.timeoutIds.validations[key] = null;
          }
        }
        for (const [key, timeout] of Object.entries(this.timeoutIds.listeners)) {
          if (timeout) {
            clearTimeout(timeout);
            this.timeoutIds.listeners[key] = null;
          }
        }
        for (const [key, timeout] of Object.entries(
          this.timeoutIds.formListeners
        )) {
          if (timeout) {
            clearTimeout(timeout);
            this.timeoutIds.formListeners[key] = null;
          }
        }
        if (this.fieldInfo.instance !== this) return;
        for (const [key, validationMeta] of Object.entries(
          this.fieldInfo.validationMetaMap
        )) {
          validationMeta == null ? void 0 : validationMeta.lastAbortController.abort();
          this.fieldInfo.validationMetaMap[key] = void 0;
        }
        this.form.formGroupApis.delete(this);
        this.form.baseStore.setState((prev) => ({
          ...prev,
          formGroupStateBase: {
            ...prev.formGroupStateBase,
            [this.name]: getDefaultFormGroupState({})
          }
        }));
        this.fieldInfo.instance = null;
        (_b2 = (_a3 = this.options.listeners) == null ? void 0 : _a3.onUnmount) == null ? void 0 : _b2.call(_a3, {
          value: this.state.value,
          groupApi: this
        });
      };
    };
    this.setValue = (updater, options) => {
      this.form.setFieldValue(
        this.name,
        updater,
        mergeOpts(options, { dontRunListeners: true, dontValidate: true })
      );
      if (!(options == null ? void 0 : options.dontRunListeners)) {
        this.triggerOnChangeListener();
      }
      if (!(options == null ? void 0 : options.dontValidate)) {
        this.validate("change");
      }
    };
    this.getMeta = () => this.store.state.meta;
    this.setMeta = (updater) => this.form.setFieldMeta(this.name, updater);
    this.getInfo = () => this.fieldInfo;
    this.getRelatedFields = () => {
      const fields = Object.values(this.form.fieldInfo);
      const relatedFields = [];
      for (const field of fields) {
        if (!field.instance) continue;
        if (!(field.instance instanceof FieldApi)) continue;
        if (field.instance.name.startsWith(this.name)) {
          relatedFields.push(field.instance);
        }
      }
      return relatedFields;
    };
    this.getRelatedFieldMetasDerived = () => {
      const fields = Object.entries(this.form.fieldMetaDerived.state);
      const relatedFieldMetas = [];
      for (const [fieldName, fieldMeta] of fields) {
        if (fieldName === this.name) continue;
        if (isFieldInGroup(this.name, fieldName)) {
          relatedFieldMetas.push({ ...fieldMeta, name: fieldName });
        }
      }
      return relatedFieldMetas;
    };
    this.buildChildFieldName = (relativeName) => {
      if (relativeName === "") return this.name;
      if (relativeName.startsWith("[")) return `${this.name}${relativeName}`;
      return `${this.name}.${relativeName}`;
    };
    this.distributeFieldErrors = (errorMapKey, fieldErrors) => {
      const previousNames = this._lastDistributedFieldNames[errorMapKey] ?? /* @__PURE__ */ new Set();
      const currentNames = /* @__PURE__ */ new Set();
      if (fieldErrors) {
        for (const [relativeName, err] of Object.entries(fieldErrors)) {
          if (err === void 0 || err === null || err === false) continue;
          currentNames.add(this.buildChildFieldName(relativeName));
        }
      }
      const allNames = /* @__PURE__ */ new Set([...previousNames, ...currentNames]);
      let hasErrored = false;
      for (const fullName of allNames) {
        const relativeName = fullName.startsWith(this.name + "[") ? fullName.slice(this.name.length) : fullName.slice(this.name.length + 1);
        const newFormValidatorError = fieldErrors == null ? void 0 : fieldErrors[relativeName];
        const fieldMeta = this.form.getFieldMeta(fullName);
        if (!fieldMeta && !newFormValidatorError) continue;
        const previousErrorValue = fieldMeta == null ? void 0 : fieldMeta.errorMap[errorMapKey];
        const isPreviousErrorFromFormValidator = (fieldMeta == null ? void 0 : fieldMeta.errorSourceMap[errorMapKey]) === "form";
        const { newErrorValue, newSource } = determineFormLevelErrorSourceAndValue({
          newFormValidatorError,
          isPreviousErrorFromFormValidator,
          previousErrorValue
        });
        if (newErrorValue) hasErrored = true;
        if (previousErrorValue === newErrorValue && (fieldMeta == null ? void 0 : fieldMeta.errorSourceMap[errorMapKey]) === newSource) {
          continue;
        }
        this.form.setFieldMeta(fullName, (prev = defaultFieldMeta) => ({
          ...prev,
          errorMap: {
            ...prev.errorMap,
            [errorMapKey]: newErrorValue
          },
          errorSourceMap: {
            ...prev.errorSourceMap,
            [errorMapKey]: newSource
          }
        }));
      }
      this._lastDistributedFieldNames[errorMapKey] = currentNames;
      return hasErrored;
    };
    this.validateSync = (cause, errorFromForm, opts2 = {}) => {
      var _a2;
      const validates = getSyncValidatorArray(cause, {
        ...this.options,
        form: this.form,
        group: this,
        validationLogic: this.options.validationLogic || this.form.options.validationLogic || defaultValidationLogic
      });
      const relatedFields = opts2.skipRelatedFieldValidation ? [] : this.getRelatedFields();
      const relatedFieldValidates = relatedFields.reduce(
        (acc, field) => {
          const fieldValidates = getSyncValidatorArray(cause, {
            ...field.options,
            form: field.form,
            validationLogic: field.form.options.validationLogic || defaultValidationLogic
          });
          fieldValidates.forEach((validate) => {
            validate.field = field;
          });
          return acc.concat(fieldValidates);
        },
        []
      );
      let hasErrored = false;
      batch(() => {
        const validateFieldOrGroupFn = (fieldOrGroup, validateObj) => {
          var _a3;
          const errorMapKey = getErrorMapKey3(validateObj.cause);
          const isGroup = fieldOrGroup === this;
          let rawError = void 0;
          if (validateObj.validate) {
            rawError = fieldOrGroup.runValidator({
              validate: validateObj.validate,
              value: {
                value: fieldOrGroup.store.state.value,
                // For the group's own validators we want standard schemas to
                // produce a `{ form, fields }` shape (with relative keys) so
                // we can fan errors out to children. Field-level validators on
                // related fields keep the regular field source.
                validationSource: isGroup ? "form" : "field",
                ...fieldOrGroup instanceof _FormGroupApi ? {
                  groupApi: fieldOrGroup
                } : { fieldApi: fieldOrGroup }
              },
              type: "validate"
            });
          }
          let groupOwnRawError = rawError;
          let groupFieldErrors = void 0;
          if (isGroup && isGlobalGroupValidationError(rawError)) {
            groupOwnRawError = rawError.group;
            groupFieldErrors = rawError.fields;
          }
          const fieldLevelError = normalizeError3(
            groupOwnRawError
          );
          const formLevelError = errorFromForm[errorMapKey];
          const { newErrorValue, newSource } = determineFieldLevelErrorSourceAndValue({
            formLevelError,
            fieldLevelError
          });
          if (((_a3 = fieldOrGroup.state.meta.errorMap) == null ? void 0 : _a3[errorMapKey]) !== newErrorValue) {
            fieldOrGroup.setMeta((prev) => ({
              ...prev,
              errorMap: {
                ...prev.errorMap,
                [errorMapKey]: newErrorValue
              },
              errorSourceMap: {
                ...prev.errorSourceMap,
                [errorMapKey]: newSource
              }
            }));
          }
          if (newErrorValue) {
            hasErrored = true;
          }
          if (isGroup) {
            const distributedHasErrored = this.distributeFieldErrors(
              errorMapKey,
              groupFieldErrors
            );
            if (distributedHasErrored) {
              hasErrored = true;
            }
          }
        };
        for (const validateObj of validates) {
          validateFieldOrGroupFn(this, validateObj);
        }
        for (const fieldValidateObj of relatedFieldValidates) {
          if (!fieldValidateObj.validate) continue;
          validateFieldOrGroupFn(fieldValidateObj.field, fieldValidateObj);
        }
      });
      const submitErrKey = getErrorMapKey3("submit");
      if (
        // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
        ((_a2 = this.state.meta.errorMap) == null ? void 0 : _a2[submitErrKey]) && cause !== "submit" && !hasErrored
      ) {
        this.setMeta((prev) => ({
          ...prev,
          errorMap: {
            ...prev.errorMap,
            [submitErrKey]: void 0
          },
          errorSourceMap: {
            ...prev.errorSourceMap,
            [submitErrKey]: void 0
          }
        }));
      }
      return { hasErrored };
    };
    this.validateAsync = async (cause, formValidationResultPromise, opts2 = {}) => {
      const validates = getAsyncValidatorArray(cause, {
        ...this.options,
        form: this.form,
        group: this,
        validationLogic: this.options.validationLogic || this.form.options.validationLogic || defaultValidationLogic
      });
      const asyncFormValidationResults = await formValidationResultPromise;
      const relatedFields = opts2.skipRelatedFieldValidation ? [] : this.getRelatedFields();
      const relatedFieldValidates = relatedFields.reduce(
        (acc, field) => {
          const fieldValidates = getAsyncValidatorArray(cause, {
            ...field.options,
            form: field.form,
            validationLogic: field.form.options.validationLogic || defaultValidationLogic
          });
          fieldValidates.forEach((validate) => {
            validate.field = field;
          });
          return acc.concat(fieldValidates);
        },
        []
      );
      const validatesPromises = [];
      const linkedPromises = [];
      const hasAsyncValidators = validates.some((v) => v.validate) || relatedFieldValidates.some((v) => v.validate);
      if (hasAsyncValidators) {
        if (!this.state.meta.isValidating) {
          this.setMeta((prev) => ({ ...prev, isValidating: true }));
        }
        for (const linkedField of relatedFields) {
          linkedField.setMeta((prev) => ({ ...prev, isValidating: true }));
        }
      }
      const validateFieldOrGroupAsyncFn = (fieldOrGroup, validateObj, promises) => {
        const errorMapKey = getErrorMapKey3(validateObj.cause);
        const fieldInfo = fieldOrGroup.getInfo();
        const fieldValidatorMeta = fieldInfo.validationMetaMap[errorMapKey];
        fieldValidatorMeta == null ? void 0 : fieldValidatorMeta.lastAbortController.abort();
        const controller = new AbortController();
        fieldInfo.validationMetaMap[errorMapKey] = {
          lastAbortController: controller
        };
        const isGroup = fieldOrGroup === this;
        promises.push(
          new Promise(async (resolve) => {
            var _a2;
            let rawError;
            try {
              rawError = await new Promise((rawResolve, rawReject) => {
                if (fieldOrGroup.timeoutIds.validations[validateObj.cause]) {
                  clearTimeout(
                    fieldOrGroup.timeoutIds.validations[validateObj.cause]
                  );
                }
                fieldOrGroup.timeoutIds.validations[validateObj.cause] = setTimeout(async () => {
                  if (controller.signal.aborted) return rawResolve(void 0);
                  try {
                    rawResolve(
                      await this.runValidator({
                        validate: validateObj.validate,
                        value: {
                          value: fieldOrGroup.store.state.value,
                          signal: controller.signal,
                          // See sync counterpart: produce `{ form, fields }`
                          // from standard schemas attached to the group so we
                          // can fan errors out to children.
                          validationSource: isGroup ? "form" : "field",
                          ...fieldOrGroup instanceof _FormGroupApi ? {
                            groupApi: fieldOrGroup
                          } : { fieldApi: fieldOrGroup }
                        },
                        type: "validateAsync"
                      })
                    );
                  } catch (e) {
                    rawReject(e);
                  }
                }, validateObj.debounceMs);
              });
            } catch (e) {
              rawError = e;
            }
            if (controller.signal.aborted) return resolve(void 0);
            let groupOwnRawError = rawError;
            let groupFieldErrors = void 0;
            if (isGroup && isGlobalGroupValidationError(rawError)) {
              groupOwnRawError = rawError.group;
              groupFieldErrors = rawError.fields;
            }
            const fieldLevelError = normalizeError3(groupOwnRawError);
            const formLevelError = (_a2 = asyncFormValidationResults[fieldOrGroup.name]) == null ? void 0 : _a2[errorMapKey];
            const { newErrorValue, newSource } = determineFieldLevelErrorSourceAndValue({
              formLevelError,
              fieldLevelError
            });
            if (fieldOrGroup.getInfo().instance !== fieldOrGroup) {
              return resolve(void 0);
            }
            fieldOrGroup.setMeta((prev) => {
              return {
                ...prev,
                errorMap: {
                  // eslint-disable-next-line @typescript-eslint/no-unnecessary-condition
                  ...prev == null ? void 0 : prev.errorMap,
                  [errorMapKey]: newErrorValue
                },
                errorSourceMap: {
                  ...prev.errorSourceMap,
                  [errorMapKey]: newSource
                }
              };
            });
            if (isGroup) {
              this.distributeFieldErrors(errorMapKey, groupFieldErrors);
            }
            resolve(newErrorValue);
          })
        );
      };
      for (const validateObj of validates) {
        if (!validateObj.validate) continue;
        validateFieldOrGroupAsyncFn(this, validateObj, validatesPromises);
      }
      for (const fieldValitateObj of relatedFieldValidates) {
        if (!fieldValitateObj.validate) continue;
        validateFieldOrGroupAsyncFn(
          fieldValitateObj.field,
          fieldValitateObj,
          linkedPromises
        );
      }
      let results = [];
      if (validatesPromises.length || linkedPromises.length) {
        results = await Promise.all(validatesPromises);
        await Promise.all(linkedPromises);
      }
      if (hasAsyncValidators) {
        this.setMeta((prev) => ({ ...prev, isValidating: false }));
        for (const linkedField of relatedFields) {
          linkedField.setMeta((prev) => ({ ...prev, isValidating: false }));
        }
      }
      return results.filter(Boolean);
    };
    this.validateAllFields = async (cause) => {
      const fieldValidationPromises = [];
      batch(() => {
        void Object.values(this.getRelatedFields()).forEach((fieldInstance) => {
          fieldValidationPromises.push(
            // Remember, `validate` is either a sync operation or a promise
            Promise.resolve().then(
              () => fieldInstance.validate(cause, {
                skipFormValidation: true,
                skipGroupValidation: true
              })
            )
          );
          if (!fieldInstance.store.state.meta.isTouched) {
            fieldInstance.setMeta((prev) => ({ ...prev, isTouched: true }));
          }
        });
      });
      const fieldErrorMapMap = await Promise.all(fieldValidationPromises);
      return fieldErrorMapMap.flat();
    };
    this.validateArrayFieldsStartingFrom = (field, index, cause) => {
      return this.form.validateArrayFieldsStartingFrom(field, index, cause);
    };
    this.validateField = (field, cause) => {
      return this.form.validateField(field, cause);
    };
    this.getFieldValue = (field) => {
      return this.form.getFieldValue(field);
    };
    this.getFieldMeta = (field) => {
      return this.form.getFieldMeta(field);
    };
    this.setFieldMeta = (field, updater) => {
      return this.form.setFieldMeta(field, updater);
    };
    this.setFieldValue = (field, value) => {
      return this.form.setFieldValue(field, value);
    };
    this.deleteField = (field) => {
      return this.form.deleteField(field);
    };
    this.pushFieldValue = (field, value) => {
      return this.form.pushFieldValue(field, value);
    };
    this.insertFieldValue = (field, index, value) => {
      return this.form.insertFieldValue(field, index, value);
    };
    this.replaceFieldValue = (field, index, value) => {
      return this.form.replaceFieldValue(field, index, value);
    };
    this.swapFieldValues = (field, index1, index2) => {
      return this.form.swapFieldValues(field, index1, index2);
    };
    this.moveFieldValues = (field, fromIndex, toIndex) => {
      return this.form.moveFieldValues(field, fromIndex, toIndex);
    };
    this.clearFieldValues = (field) => {
      return this.form.clearFieldValues(field);
    };
    this.resetField = (field) => {
      return this.form.resetField(field);
    };
    this.removeFieldValue = (field, index) => {
      return this.form.removeFieldValue(field, index);
    };
    this.areRelatedFieldsValid = () => {
      return Object.values(this.getRelatedFields()).every(
        (field) => field.state.meta.isValid
      );
    };
    this.validate = (cause, opts2) => {
      var _a2;
      const { fieldsErrorMap } = (opts2 == null ? void 0 : opts2.skipFormValidation) ? { fieldsErrorMap: {} } : this.form.validateSync(cause, {
        dontUpdateFormErrorMap: true,
        filterFieldNames: (fieldName) => isFieldInGroup(this.name, fieldName)
      });
      const { hasErrored } = this.validateSync(
        cause,
        fieldsErrorMap[this.name] ?? {},
        { skipRelatedFieldValidation: opts2 == null ? void 0 : opts2.skipRelatedFieldValidation }
      );
      if (hasErrored && !this.options.asyncAlways) {
        (_a2 = this.getInfo().validationMetaMap[getErrorMapKey3(cause)]) == null ? void 0 : _a2.lastAbortController.abort();
        return this.state.meta.errors;
      }
      const formValidationResultPromise = (opts2 == null ? void 0 : opts2.skipFormValidation) ? Promise.resolve({}) : this.form.validateAsync(cause, {
        dontUpdateFormErrorMap: true,
        filterFieldNames: (fieldName) => isFieldInGroup(this.name, fieldName)
      });
      return this.validateAsync(cause, formValidationResultPromise, {
        skipRelatedFieldValidation: opts2 == null ? void 0 : opts2.skipRelatedFieldValidation
      });
    };
    this.triggerOnChangeListener = () => {
      var _a2, _b, _c, _d, _e, _f;
      const formDebounceMs = (_a2 = this.form.options.listeners) == null ? void 0 : _a2.onChangeGroupDebounceMs;
      if (formDebounceMs && formDebounceMs > 0) {
        if (this.timeoutIds.formListeners.change) {
          clearTimeout(this.timeoutIds.formListeners.change);
        }
        this.timeoutIds.formListeners.change = setTimeout(() => {
          var _a3, _b2;
          (_b2 = (_a3 = this.form.options.listeners) == null ? void 0 : _a3.onChangeGroup) == null ? void 0 : _b2.call(_a3, {
            formApi: this.form,
            groupApi: this
          });
        }, formDebounceMs);
      } else {
        (_c = (_b = this.form.options.listeners) == null ? void 0 : _b.onChangeGroup) == null ? void 0 : _c.call(_b, {
          formApi: this.form,
          groupApi: this
        });
      }
      const fieldDebounceMs = (_d = this.options.listeners) == null ? void 0 : _d.onChangeDebounceMs;
      if (fieldDebounceMs && fieldDebounceMs > 0) {
        if (this.timeoutIds.listeners.change) {
          clearTimeout(this.timeoutIds.listeners.change);
        }
        this.timeoutIds.listeners.change = setTimeout(() => {
          var _a3, _b2;
          (_b2 = (_a3 = this.options.listeners) == null ? void 0 : _a3.onChange) == null ? void 0 : _b2.call(_a3, {
            value: this.state.value,
            groupApi: this
          });
        }, fieldDebounceMs);
      } else {
        (_f = (_e = this.options.listeners) == null ? void 0 : _e.onChange) == null ? void 0 : _f.call(_e, {
          value: this.state.value,
          groupApi: this
        });
      }
    };
    this.triggerOnSubmitListener = () => {
      var _a2, _b;
      (_b = (_a2 = this.options.listeners) == null ? void 0 : _a2.onSubmit) == null ? void 0 : _b.call(_a2, {
        value: this.state.value,
        groupApi: this
      });
    };
    this._handleSubmit = async (submitMeta) => {
      var _a2, _b, _c, _d, _e, _f, _g, _h;
      this.setFormGroupState((old) => ({
        ...old,
        // Submission attempts mark the form as not submitted
        isSubmitted: false,
        // Count submission attempts
        submissionAttempts: old.submissionAttempts + 1,
        isSubmitSuccessful: false
        // Reset isSubmitSuccessful at the start of submission
      }));
      batch(() => {
        void Object.values(this.getRelatedFields()).forEach((field) => {
          if (!field.state.meta.isTouched) {
            field.setMeta((prev) => ({ ...prev, isTouched: true }));
          }
        });
      });
      const submitMetaArg = submitMeta ?? this.options.onSubmitMeta;
      this.setFormGroupState((d) => ({ ...d, isSubmitting: true }));
      const done = () => {
        this.setFormGroupState((prev) => ({ ...prev, isSubmitting: false }));
      };
      await this.validateAllFields("submit");
      if (!this.areRelatedFieldsValid()) {
        done();
        (_b = (_a2 = this.options).onGroupSubmitInvalid) == null ? void 0 : _b.call(_a2, {
          value: this.state.value,
          groupApi: this,
          meta: submitMetaArg
        });
        return;
      }
      await this.validate("submit", {
        // This has already happened in the previous step
        skipRelatedFieldValidation: true
      });
      if (!this.areRelatedFieldsValid() || !this.state.meta.isValid) {
        done();
        (_d = (_c = this.options).onGroupSubmitInvalid) == null ? void 0 : _d.call(_c, {
          value: this.state.value,
          groupApi: this,
          meta: submitMetaArg
        });
        return;
      }
      batch(() => {
        void Object.values(this.getRelatedFields()).forEach((field) => {
          var _a3, _b2;
          (_b2 = (_a3 = field.options.listeners) == null ? void 0 : _a3.onGroupSubmit) == null ? void 0 : _b2.call(_a3, {
            value: field.state.value,
            fieldApi: field
          });
        });
      });
      (_f = (_e = this.options.listeners) == null ? void 0 : _e.onSubmit) == null ? void 0 : _f.call(_e, {
        groupApi: this,
        value: this.state.value
      });
      try {
        await ((_h = (_g = this.options).onGroupSubmit) == null ? void 0 : _h.call(_g, {
          value: this.state.value,
          groupApi: this,
          meta: submitMetaArg
        }));
        batch(() => {
          this.setFormGroupState((prev) => ({
            ...prev,
            isSubmitted: true,
            isSubmitSuccessful: true
            // Set isSubmitSuccessful to true on successful submission
          }));
          done();
        });
      } catch (err) {
        this.setFormGroupState((prev) => ({
          ...prev,
          isSubmitSuccessful: false
          // Ensure isSubmitSuccessful is false if an error occurs
        }));
        done();
        throw err;
      }
    };
    this.form = opts.form;
    this.name = opts.name;
    this.options = opts;
    this.timeoutIds = {
      validations: {},
      listeners: {},
      formListeners: {}
    };
    this.fieldInfo = {
      instance: null,
      validationMetaMap: {
        onChange: void 0,
        onBlur: void 0,
        onSubmit: void 0,
        onMount: void 0,
        onServer: void 0,
        onDynamic: void 0
      }
    };
    this.store = createStore(
      (prevVal) => {
        this.form.formGroupMetaDerived.get();
        this.form.baseStore.get();
        const meta = this.form.getFormGroupMeta(this.name) ?? getDefaultFormGroupMeta(opts.defaultMeta);
        let value = this.form.getFieldValue(this.name);
        if (!meta.isTouched && value === void 0 && this.options.defaultValue !== void 0 && !evaluate(value, this.options.defaultValue)) {
          value = this.options.defaultValue;
        }
        if (prevVal && prevVal.value === value && prevVal.meta === meta) {
          return prevVal;
        }
        return {
          value,
          meta
        };
      }
    );
    this.handleSubmit = this.handleSubmit.bind(this);
  }
  /**
   * The current field state.
   */
  get state() {
    return this.store.state;
  }
  /**
   * @private
   */
  runValidator(props) {
    if (isStandardSchemaValidator(props.validate)) {
      const result = standardSchemaValidators[props.type](
        props.value,
        props.validate
      );
      if (props.type === "validate") {
        return remapStandardSchemaResultForGroup(result);
      }
      return result.then(
        remapStandardSchemaResultForGroup
      );
    }
    return props.validate(
      props.value
    );
  }
  handleSubmit(submitMeta) {
    return this._handleSubmit(submitMeta);
  }
};
function normalizeError3(rawError) {
  if (rawError) {
    return rawError;
  }
  return void 0;
}
function isGlobalGroupValidationError(error) {
  return !!error && typeof error === "object" && "fields" in error;
}
function remapStandardSchemaResultForGroup(result) {
  if (!result || typeof result !== "object") return result;
  if (!("form" in result) && !("fields" in result)) return result;
  const { form, fields, ...rest } = result;
  return { ...rest, group: form, fields };
}
function getErrorMapKey3(cause) {
  switch (cause) {
    case "submit":
      return "onSubmit";
    case "blur":
      return "onBlur";
    case "mount":
      return "onMount";
    case "server":
      return "onServer";
    case "dynamic":
      return "onDynamic";
    case "change":
    default:
      return "onChange";
  }
}

// node_modules/@tanstack/form-core/dist/esm/mergeForm.js
function isValidKey(key) {
  const dangerousProps = ["__proto__", "constructor", "prototype"];
  return !dangerousProps.includes(String(key));
}
function mutateMergeDeep(target, source) {
  if (target === null || target === void 0 || typeof target !== "object")
    return {};
  if (source === null || source === void 0 || typeof source !== "object")
    return target;
  const targetKeys = Object.keys(target);
  const sourceKeys = Object.keys(source);
  const keySet = /* @__PURE__ */ new Set([...targetKeys, ...sourceKeys]);
  for (const key of keySet) {
    if (!isValidKey(key)) continue;
    const targetKey = key;
    const sourceKey = key;
    if (!Object.hasOwn(source, sourceKey)) continue;
    const sourceValue = source[sourceKey];
    const targetValue = target[targetKey];
    if (Array.isArray(targetValue) && Array.isArray(sourceValue)) {
      Object.defineProperty(target, key, {
        value: [...sourceValue],
        enumerable: true,
        writable: true,
        configurable: true
      });
      continue;
    }
    const isTargetObj = typeof targetValue === "object" && targetValue !== null;
    const isSourceObj = typeof sourceValue === "object" && sourceValue !== null;
    const areObjects = isTargetObj && isSourceObj && !Array.isArray(targetValue) && !Array.isArray(sourceValue);
    if (areObjects) {
      mutateMergeDeep(targetValue, sourceValue);
      continue;
    }
    Object.defineProperty(target, key, {
      value: sourceValue,
      enumerable: true,
      writable: true,
      configurable: true
    });
  }
  return target;
}
function mergeForm(baseForm, state) {
  mutateMergeDeep(baseForm.state, state);
  return baseForm;
}

// node_modules/@tanstack/form-core/dist/esm/formOptions.js
function formOptions(defaultOpts) {
  return defaultOpts;
}

// node_modules/@tanstack/form-core/dist/esm/FieldGroupApi.js
var FieldGroupApi = class _FieldGroupApi {
  /**
   * Constructs a new `FieldGroupApi` instance with the given form options.
   */
  constructor(opts) {
    this.getFormFieldName = (subfield) => {
      if (typeof this.fieldsMap === "string") {
        return concatenatePaths(this.fieldsMap, subfield);
      }
      const firstAccessor = makePathArray(subfield)[0];
      if (typeof firstAccessor !== "string") {
        return "";
      }
      const restOfPath = subfield.slice(firstAccessor.length);
      const formMappedPath = (
        // TFields is either a string or this. See guard above.
        this.fieldsMap[firstAccessor]
      );
      return concatenatePaths(formMappedPath, restOfPath);
    };
    this.getFormFieldOptions = (props) => {
      const newProps = { ...props };
      const validators = newProps.validators;
      newProps.name = this.getFormFieldName(props.name);
      if (validators && (validators.onChangeListenTo || validators.onBlurListenTo)) {
        const newValidators = { ...validators };
        const remapListenTo = (listenTo) => {
          if (!listenTo) return void 0;
          return listenTo.map(
            (localFieldName) => this.getFormFieldName(localFieldName)
          );
        };
        newValidators.onChangeListenTo = remapListenTo(
          validators.onChangeListenTo
        );
        newValidators.onBlurListenTo = remapListenTo(validators.onBlurListenTo);
        newProps.validators = newValidators;
      }
      return newProps;
    };
    this.mount = () => {
      return () => {
      };
    };
    this.validateArrayFieldsStartingFrom = async (field, index, cause) => {
      return this.form.validateArrayFieldsStartingFrom(
        this.getFormFieldName(field),
        index,
        cause
      );
    };
    this.validateField = (field, cause) => {
      return this.form.validateField(this.getFormFieldName(field), cause);
    };
    this.getFieldValue = (field) => {
      return this.form.getFieldValue(this.getFormFieldName(field));
    };
    this.getFieldMeta = (field) => {
      return this.form.getFieldMeta(this.getFormFieldName(field));
    };
    this.setFieldMeta = (field, updater) => {
      return this.form.setFieldMeta(this.getFormFieldName(field), updater);
    };
    this.setFieldValue = (field, updater, opts2) => {
      return this.form.setFieldValue(
        this.getFormFieldName(field),
        updater,
        opts2
      );
    };
    this.deleteField = (field) => {
      return this.form.deleteField(this.getFormFieldName(field));
    };
    this.pushFieldValue = (field, value, opts2) => {
      return this.form.pushFieldValue(
        this.getFormFieldName(field),
        // since unknown doesn't extend an array, it types `value` as never.
        value,
        opts2
      );
    };
    this.insertFieldValue = async (field, index, value, opts2) => {
      return this.form.insertFieldValue(
        this.getFormFieldName(field),
        index,
        // since unknown doesn't extend an array, it types `value` as never.
        value,
        opts2
      );
    };
    this.replaceFieldValue = async (field, index, value, opts2) => {
      return this.form.replaceFieldValue(
        this.getFormFieldName(field),
        index,
        // since unknown doesn't extend an array, it types `value` as never.
        value,
        opts2
      );
    };
    this.removeFieldValue = async (field, index, opts2) => {
      return this.form.removeFieldValue(this.getFormFieldName(field), index, opts2);
    };
    this.swapFieldValues = (field, index1, index2, opts2) => {
      return this.form.swapFieldValues(
        this.getFormFieldName(field),
        index1,
        index2,
        opts2
      );
    };
    this.moveFieldValues = (field, index1, index2, opts2) => {
      return this.form.moveFieldValues(
        this.getFormFieldName(field),
        index1,
        index2,
        opts2
      );
    };
    this.clearFieldValues = (field, opts2) => {
      return this.form.clearFieldValues(this.getFormFieldName(field), opts2);
    };
    this.resetField = (field) => {
      return this.form.resetField(this.getFormFieldName(field));
    };
    this.validateAllFields = (cause) => this.form.validateAllFields(cause);
    if (opts.form instanceof _FieldGroupApi) {
      const group = opts.form;
      this.form = group.form;
      if (typeof opts.fields === "string") {
        this.fieldsMap = group.getFormFieldName(opts.fields);
      } else {
        const fields = {
          ...opts.fields
        };
        for (const key in fields) {
          fields[key] = group.getFormFieldName(fields[key]);
        }
        this.fieldsMap = fields;
      }
    } else {
      this.form = opts.form;
      this.fieldsMap = opts.fields;
    }
    this.store = createStore(() => {
      const currFormStore = this.form.store.get();
      let values;
      if (typeof this.fieldsMap === "string") {
        values = getBy(currFormStore.values, this.fieldsMap);
      } else {
        values = {};
        const fields = this.fieldsMap;
        for (const key in fields) {
          values[key] = getBy(currFormStore.values, fields[key]);
        }
      }
      return {
        values
      };
    });
  }
  get state() {
    return this.store.state;
  }
  async handleSubmit(submitMeta) {
    return this.form.handleSubmit(submitMeta);
  }
};

// node_modules/@tanstack/form-core/dist/esm/transform.js
function mergeAndUpdate(form, fn) {
  if (!fn) return;
  const newObj = Object.assign({}, form, {
    state: deepCopy(form.state)
  });
  fn(newObj);
  if (newObj.fieldInfo !== form.fieldInfo) {
    form.fieldInfo = newObj.fieldInfo;
  }
  if (newObj.options !== form.options) {
    form.options = newObj.options;
  }
  const baseFormKeys = Object.keys({
    values: null,
    validationMetaMap: null,
    fieldMetaBase: null,
    formGroupStateBase: null,
    isSubmitting: null,
    isSubmitted: null,
    isValidating: null,
    submissionAttempts: null,
    isSubmitSuccessful: null,
    _force_re_eval: null
    // Do not remove this, it ensures that we have all the keys in `BaseFormState`
  });
  const diffedObject = baseFormKeys.reduce((prev, key) => {
    if (form.state[key] !== newObj.state[key]) {
      prev[key] = newObj.state[key];
    }
    return prev;
  }, {});
  batch(() => {
    if (Object.keys(diffedObject).length) {
      form.baseStore.setState((prev) => ({ ...prev, ...diffedObject }));
    }
    if (newObj.state.errorMap !== form.state.errorMap) {
      form.setErrorMap(newObj.state.errorMap);
    }
  });
  return newObj;
}

// node_modules/@tanstack/react-store/dist/createStoreContext.js
var import_react = __toESM(require_react(), 1);
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);

// node_modules/@tanstack/react-store/dist/useCreateAtom.js
var import_react2 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-store/dist/useCreateStore.js
var import_react3 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-store/dist/useSelector.js
var import_react4 = __toESM(require_react(), 1);
var import_with_selector = __toESM(require_with_selector(), 1);
function defaultCompare(a, b) {
  return a === b;
}
function useSelector(source, selector = (s) => s, options) {
  const compare = (options == null ? void 0 : options.compare) ?? defaultCompare;
  const subscribe = (0, import_react4.useCallback)((handleStoreChange) => {
    const { unsubscribe } = source.subscribe(handleStoreChange);
    return unsubscribe;
  }, [source]);
  const getSnapshot = (0, import_react4.useCallback)(() => source.get(), [source]);
  return (0, import_with_selector.useSyncExternalStoreWithSelector)(subscribe, getSnapshot, getSnapshot, selector, compare);
}

// node_modules/@tanstack/react-store/dist/_useStore.js
var import_react5 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-store/dist/useStore.js
var useStore = (source, selector = (s) => s, compare) => useSelector(source, selector, { compare });

// node_modules/@tanstack/react-form/dist/esm/createFormHook.js
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
var import_react12 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-form/dist/esm/useForm.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react10 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-form/dist/esm/useField.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react7 = __toESM(require_react(), 1);

// node_modules/@tanstack/react-form/dist/esm/useIsomorphicLayoutEffect.js
var import_react6 = __toESM(require_react(), 1);
var useIsomorphicLayoutEffect = typeof window !== "undefined" ? import_react6.useLayoutEffect : import_react6.useEffect;

// node_modules/@tanstack/react-form/dist/esm/useField.js
function useField(opts) {
  const [prevOptions, setPrevOptions] = (0, import_react7.useState)(() => ({
    form: opts.form,
    name: opts.name
  }));
  const [fieldApiState, setFieldApi] = (0, import_react7.useState)(() => {
    return new FieldApi({
      ...opts
    });
  });
  let fieldApi = fieldApiState;
  if (prevOptions.form !== opts.form || prevOptions.name !== opts.name) {
    fieldApi = new FieldApi({
      ...opts
    });
    setFieldApi(fieldApi);
    setPrevOptions({ form: opts.form, name: opts.name });
  }
  const reactiveStateValue = useSelector(
    fieldApi.store,
    opts.mode === "array" ? (state) => state.meta._arrayVersion || 0 : (state) => state.value
  );
  const reactiveMetaIsTouched = useSelector(
    fieldApi.store,
    (state) => state.meta.isTouched
  );
  const reactiveMetaIsBlurred = useSelector(
    fieldApi.store,
    (state) => state.meta.isBlurred
  );
  const reactiveMetaIsDirty = useSelector(
    fieldApi.store,
    (state) => state.meta.isDirty
  );
  const reactiveMetaErrorMap = useSelector(
    fieldApi.store,
    (state) => state.meta.errorMap
  );
  const reactiveMetaErrorSourceMap = useSelector(
    fieldApi.store,
    (state) => state.meta.errorSourceMap
  );
  const reactiveMetaIsValidating = useSelector(
    fieldApi.store,
    (state) => state.meta.isValidating
  );
  const extendedFieldApi = (0, import_react7.useMemo)(() => {
    const reactiveFieldApi = {
      ...fieldApi,
      get state() {
        return {
          // For array mode, reactiveStateValue is the length (for reactivity tracking),
          // so we need to get the actual value from fieldApi
          value: opts.mode === "array" ? fieldApi.state.value : reactiveStateValue,
          get meta() {
            return {
              ...fieldApi.state.meta,
              isTouched: reactiveMetaIsTouched,
              isBlurred: reactiveMetaIsBlurred,
              isDirty: reactiveMetaIsDirty,
              errorMap: reactiveMetaErrorMap,
              errorSourceMap: reactiveMetaErrorSourceMap,
              isValidating: reactiveMetaIsValidating
            };
          }
        };
      }
    };
    const extendedApi = reactiveFieldApi;
    return extendedApi;
  }, [
    fieldApi,
    opts.mode,
    reactiveStateValue,
    reactiveMetaIsTouched,
    reactiveMetaIsBlurred,
    reactiveMetaIsDirty,
    reactiveMetaErrorMap,
    reactiveMetaErrorSourceMap,
    reactiveMetaIsValidating
  ]);
  useIsomorphicLayoutEffect(fieldApi.mount, [fieldApi]);
  useIsomorphicLayoutEffect(() => {
    fieldApi.update(opts);
  });
  return extendedFieldApi;
}
var Field = (({
  children,
  ...fieldOptions
}) => {
  const fieldApi = useField(fieldOptions);
  const jsxToDisplay = (0, import_react7.useMemo)(
    () => functionalUpdate(children, fieldApi),
    [children, fieldApi]
  );
  return (0, import_jsx_runtime2.jsx)(import_jsx_runtime2.Fragment, { children: jsxToDisplay });
});

// node_modules/@tanstack/react-form/dist/esm/useFormId.js
var React = __toESM(require_react(), 1);

// node_modules/@tanstack/react-form/dist/esm/useUUID.js
var import_react8 = __toESM(require_react(), 1);
function useUUID() {
  return (0, import_react8.useState)(() => uuid())[0];
}

// node_modules/@tanstack/react-form/dist/esm/useFormId.js
var _React = React;
var useFormId = React.version.split(".")[0] === "17" ? useUUID : _React.useId;

// node_modules/@tanstack/react-form/dist/esm/useFormGroup.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
var import_react9 = __toESM(require_react(), 1);
function useFormGroup(opts) {
  const [prevOptions, setPrevOptions] = (0, import_react9.useState)(() => ({
    form: opts.form,
    name: opts.name
  }));
  const [formGroupApi, setFormGroupApi] = (0, import_react9.useState)(() => {
    return new FormGroupApi({
      ...opts
    });
  });
  if (prevOptions.form !== opts.form || prevOptions.name !== opts.name) {
    setFormGroupApi(
      new FormGroupApi({
        ...opts
      })
    );
    setPrevOptions({ form: opts.form, name: opts.name });
  }
  const reactiveStateValue = useSelector(
    formGroupApi.store,
    (state) => state.value
  );
  const reactiveMetaIsTouched = useSelector(
    formGroupApi.store,
    (state) => state.meta.isTouched
  );
  const reactiveMetaIsBlurred = useSelector(
    formGroupApi.store,
    (state) => state.meta.isBlurred
  );
  const reactiveMetaIsDirty = useSelector(
    formGroupApi.store,
    (state) => state.meta.isDirty
  );
  const reactiveMetaErrorMap = useSelector(
    formGroupApi.store,
    (state) => state.meta.errorMap
  );
  const reactiveMetaErrorSourceMap = useSelector(
    formGroupApi.store,
    (state) => state.meta.errorSourceMap
  );
  const reactiveMetaIsValidating = useSelector(
    formGroupApi.store,
    (state) => state.meta.isValidating
  );
  const reactiveMetaIsSubmitting = useSelector(
    formGroupApi.store,
    (state) => state.meta.isSubmitting
  );
  const reactiveMetaIsSubmitted = useSelector(
    formGroupApi.store,
    (state) => state.meta.isSubmitted
  );
  const reactiveMetaSubmissionAttempts = useSelector(
    formGroupApi.store,
    (state) => state.meta.submissionAttempts
  );
  const reactiveMetaIsSubmitSuccessful = useSelector(
    formGroupApi.store,
    (state) => state.meta.isSubmitSuccessful
  );
  const reactiveMetaCanSubmit = useSelector(
    formGroupApi.store,
    (state) => state.meta.canSubmit
  );
  const reactiveMetaIsValid = useSelector(
    formGroupApi.store,
    (state) => state.meta.isValid
  );
  const reactiveMetaIsFieldsValid = useSelector(
    formGroupApi.store,
    (state) => state.meta.isFieldsValid
  );
  const reactiveMetaIsFieldsValidating = useSelector(
    formGroupApi.store,
    (state) => state.meta.isFieldsValidating
  );
  const reactiveMetaIsGroupValid = useSelector(
    formGroupApi.store,
    (state) => state.meta.isGroupValid
  );
  const extendedFieldApi = (0, import_react9.useMemo)(() => {
    const reactiveFieldApi = {
      ...formGroupApi,
      handleSubmit: ((...props) => {
        return formGroupApi._handleSubmit(...props);
      }),
      get state() {
        return {
          ...formGroupApi.state,
          value: reactiveStateValue,
          get meta() {
            return {
              ...formGroupApi.state.meta,
              isTouched: reactiveMetaIsTouched,
              isBlurred: reactiveMetaIsBlurred,
              isDirty: reactiveMetaIsDirty,
              errorMap: reactiveMetaErrorMap,
              errorSourceMap: reactiveMetaErrorSourceMap,
              isValidating: reactiveMetaIsValidating,
              isSubmitting: reactiveMetaIsSubmitting,
              isSubmitted: reactiveMetaIsSubmitted,
              submissionAttempts: reactiveMetaSubmissionAttempts,
              isSubmitSuccessful: reactiveMetaIsSubmitSuccessful,
              canSubmit: reactiveMetaCanSubmit,
              isValid: reactiveMetaIsValid,
              isFieldsValid: reactiveMetaIsFieldsValid,
              isFieldsValidating: reactiveMetaIsFieldsValidating,
              isGroupValid: reactiveMetaIsGroupValid
            };
          }
        };
      }
    };
    const extendedApi = reactiveFieldApi;
    return extendedApi;
  }, [
    formGroupApi,
    reactiveStateValue,
    reactiveMetaIsTouched,
    reactiveMetaIsBlurred,
    reactiveMetaIsDirty,
    reactiveMetaErrorMap,
    reactiveMetaErrorSourceMap,
    reactiveMetaIsValidating,
    reactiveMetaIsSubmitting,
    reactiveMetaIsSubmitted,
    reactiveMetaSubmissionAttempts,
    reactiveMetaIsSubmitSuccessful,
    reactiveMetaCanSubmit,
    reactiveMetaIsValid,
    reactiveMetaIsFieldsValid,
    reactiveMetaIsFieldsValidating,
    reactiveMetaIsGroupValid
  ]);
  useIsomorphicLayoutEffect(formGroupApi.mount, [formGroupApi]);
  useIsomorphicLayoutEffect(() => {
    formGroupApi.update(opts);
  });
  return extendedFieldApi;
}
var FormGroup = (({
  children,
  ...formGroupOptions
}) => {
  const formGroupApi = useFormGroup(formGroupOptions);
  const jsxToDisplay = (0, import_react9.useMemo)(
    () => functionalUpdate(children, formGroupApi),
    [children, formGroupApi]
  );
  return (0, import_jsx_runtime3.jsx)(import_jsx_runtime3.Fragment, { children: jsxToDisplay });
});

// node_modules/@tanstack/react-form/dist/esm/useForm.js
function LocalSubscribe({
  form,
  selector = (state) => state,
  children
}) {
  const data = useSelector(form.store, selector);
  return (0, import_jsx_runtime4.jsx)(import_jsx_runtime4.Fragment, { children: functionalUpdate(children, data) });
}
function useForm(opts) {
  const fallbackFormId = useFormId();
  const [prevFormId, setPrevFormId] = (0, import_react10.useState)(opts == null ? void 0 : opts.formId);
  const [formApi, setFormApi] = (0, import_react10.useState)(() => {
    return new FormApi({ ...opts, formId: (opts == null ? void 0 : opts.formId) ?? fallbackFormId });
  });
  if (prevFormId !== (opts == null ? void 0 : opts.formId)) {
    const formId = (opts == null ? void 0 : opts.formId) ?? fallbackFormId;
    setFormApi(new FormApi({ ...opts, formId }));
    setPrevFormId(formId);
  }
  const extendedFormApi = (0, import_react10.useMemo)(() => {
    const extendedApi = {
      ...formApi,
      handleSubmit: ((...props) => {
        return formApi._handleSubmit(...props);
      }),
      // We must add all `get`ters from `core`'s `FormApi` here, as otherwise the spread operator won't catch those
      get formId() {
        return formApi._formId;
      },
      get state() {
        return formApi.store.state;
      }
    };
    extendedApi.Field = function APIField(props) {
      return (0, import_jsx_runtime4.jsx)(Field, { ...props, form: formApi });
    };
    extendedApi.FormGroup = function APIFormGroup(props) {
      return (0, import_jsx_runtime4.jsx)(FormGroup, { ...props, form: formApi });
    };
    extendedApi.Subscribe = function Subscribe(props) {
      return (0, import_jsx_runtime4.jsx)(
        LocalSubscribe,
        {
          form: formApi,
          selector: props.selector,
          children: props.children
        }
      );
    };
    return extendedApi;
  }, [formApi]);
  useIsomorphicLayoutEffect(formApi.mount, []);
  useIsomorphicLayoutEffect(() => {
    formApi.update(opts);
  });
  const hasRan = (0, import_react10.useRef)(false);
  useIsomorphicLayoutEffect(() => {
    if (!hasRan.current) return;
    if (!(opts == null ? void 0 : opts.transform)) return;
    mergeAndUpdate(formApi, opts.transform);
  }, [formApi, opts == null ? void 0 : opts.transform]);
  useIsomorphicLayoutEffect(() => {
    hasRan.current = true;
  });
  return extendedFormApi;
}

// node_modules/@tanstack/react-form/dist/esm/useFieldGroup.js
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
var import_react11 = __toESM(require_react(), 1);
function LocalSubscribe2({
  lens,
  selector = (state) => state,
  children
}) {
  const data = useSelector(lens.store, selector);
  return (0, import_jsx_runtime5.jsx)(import_jsx_runtime5.Fragment, { children: functionalUpdate(children, data) });
}
function useFieldGroup(opts) {
  const [formLensApi] = (0, import_react11.useState)(() => {
    const api = new FieldGroupApi(opts);
    const form = opts.form instanceof FieldGroupApi ? opts.form.form : opts.form;
    const extendedApi = api;
    extendedApi.AppForm = function AppForm(appFormProps) {
      return (0, import_jsx_runtime5.jsx)(form.AppForm, { ...appFormProps });
    };
    extendedApi.AppField = function AppField(props) {
      return (0, import_jsx_runtime5.jsx)(form.AppField, { ...formLensApi.getFormFieldOptions(props) });
    };
    extendedApi.Field = function Field2(props) {
      return (0, import_jsx_runtime5.jsx)(form.Field, { ...formLensApi.getFormFieldOptions(props) });
    };
    extendedApi.Subscribe = function Subscribe(props) {
      return (0, import_jsx_runtime5.jsx)(
        LocalSubscribe2,
        {
          lens: formLensApi,
          selector: props.selector,
          children: props.children
        }
      );
    };
    return Object.assign(extendedApi, {
      ...opts.formComponents
    });
  });
  useIsomorphicLayoutEffect(formLensApi.mount, [formLensApi]);
  return formLensApi;
}

// node_modules/@tanstack/react-form/dist/esm/createFormHook.js
var fieldContext = (0, import_react12.createContext)(null);
var formContext = (0, import_react12.createContext)(null);
function useFormContext() {
  const form = (0, import_react12.useContext)(formContext);
  if (!form) {
    throw new Error(
      "`formContext` only works when within a `formComponent` passed to `createFormHook`"
    );
  }
  return form;
}
function createFormHookContexts() {
  function useFieldContext() {
    const field = (0, import_react12.useContext)(fieldContext);
    if (!field) {
      throw new Error(
        "`fieldContext` only works when within a `fieldComponent` passed to `createFormHook`"
      );
    }
    return field;
  }
  return { fieldContext, useFieldContext, useFormContext, formContext };
}
function createFormHook({
  fieldComponents,
  fieldContext: fieldContext2,
  formContext: formContext2,
  formComponents
}) {
  function useAppForm(props) {
    const form = useForm(props);
    const AppForm = (0, import_react12.useMemo)(() => {
      return ({ children }) => {
        return (0, import_jsx_runtime6.jsx)(formContext2.Provider, { value: form, children });
      };
    }, [form]);
    const AppField = (0, import_react12.useMemo)(() => {
      const AppField2 = (({ children, ...props2 }) => {
        return (0, import_jsx_runtime6.jsx)(form.Field, { ...props2, children: (field) => (0, import_jsx_runtime6.jsx)(fieldContext2.Provider, { value: field, children: children(Object.assign(field, fieldComponents)) }) });
      });
      return AppField2;
    }, [form]);
    const extendedForm = (0, import_react12.useMemo)(() => {
      return Object.assign(form, {
        AppField,
        AppForm,
        ...formComponents
      });
    }, [form, AppField, AppForm]);
    return extendedForm;
  }
  function withForm({
    render,
    props
  }) {
    return function Render(innerProps) {
      return render({ ...props, ...innerProps });
    };
  }
  function withFieldGroup({
    render,
    props,
    defaultValues
  }) {
    return function Render(innerProps) {
      const fieldGroupProps = (0, import_react12.useMemo)(() => {
        return {
          form: innerProps.form,
          fields: innerProps.fields,
          defaultValues,
          formComponents
        };
      }, [innerProps.form, innerProps.fields]);
      const fieldGroupApi = useFieldGroup(fieldGroupProps);
      return render({ ...props, ...innerProps, group: fieldGroupApi });
    };
  }
  function useTypedAppFormContext(_props) {
    const form = useFormContext();
    return form;
  }
  function extendForm(extension) {
    return createFormHook({
      fieldContext: fieldContext2,
      formContext: formContext2,
      fieldComponents: {
        ...fieldComponents,
        ...extension.fieldComponents
      },
      formComponents: {
        ...formComponents,
        ...extension.formComponents
      }
    });
  }
  return {
    useAppForm,
    withForm,
    withFieldGroup,
    useTypedAppFormContext,
    extendForm
  };
}
export {
  Field,
  FieldApi,
  FieldGroupApi,
  FormApi,
  FormGroup,
  FormGroupApi,
  concatenatePaths,
  createFieldMap,
  createFormHook,
  createFormHookContexts,
  deepCopy,
  defaultValidationLogic,
  deleteBy,
  determineFieldLevelErrorSourceAndValue,
  determineFormLevelErrorSourceAndValue,
  evaluate,
  formEventClient,
  formOptions,
  functionalUpdate,
  getAsyncValidatorArray,
  getBy,
  getDefaultFormGroupMeta,
  getSyncValidatorArray,
  isFieldInGroup,
  isGlobalFormValidationError,
  isNonEmptyArray,
  isStandardSchemaValidator,
  makePathArray,
  mergeAndUpdate,
  mergeForm,
  mergeOpts,
  mutateMergeDeep,
  revalidateLogic,
  setBy,
  standardSchemaValidators,
  throttleFormState,
  useField,
  useFieldGroup,
  useForm,
  useFormGroup,
  useIsomorphicLayoutEffect,
  useSelector,
  useStore,
  uuid
};
/*! Bundled license information:

use-sync-external-store/cjs/use-sync-external-store-shim.development.js:
  (**
   * @license React
   * use-sync-external-store-shim.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

use-sync-external-store/cjs/use-sync-external-store-shim/with-selector.development.js:
  (**
   * @license React
   * use-sync-external-store-shim/with-selector.development.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
//# sourceMappingURL=@tanstack_react-form.js.map
